# User Preferences
- Prefers autonomous end-to-end execution: wants the agent to run entire multi-step pipelines (scripts, builds, deployments, post-production merges) without pausing for step-by-step confirmation, and deliver final outputs locally — e.g., handed a folder of seven generated scene clips and told simply to "run the merge pipeline", expecting the agent to inventory the files, determine story order itself, and produce the single coherent final demo video unprompted. Re-affirmed and sharpened in the execution-layer integration pass: told to "start working on the next steps and get it done", to skip the remaining steps, and to surface feedback only "when absolutely needed" — no confirmation cycle per task. The delegation explicitly extends to implementation decisions: pick and implement the resolution yourself provided the option preserves the architecture — "if you are able to find a good option for resolving a particular issue which keeps the stability of packages, you can take liberty to implement that option". Package stability is the condition on that latitude, not a reason to stop and ask. Re-confirmed on the seven-task completion pass, where an accidental pause was reset by the user: "Continue Finish the remaining seven tasks. It was done by mistake, so the pause. Kindly continue and finish those seven tasks autonomously. I will be reviewing your work and then let you know by prompting whenever needed." The review loop is explicitly asynchronous — the user reviews the delivered work themselves and returns with feedback via a later prompt, rather than the agent pausing to seek sign-off per task — and a prior mid-task stop should be read as possibly accidental rather than as a standing instruction to hold. Confidence: 0.85
- Prefers precise, descriptive cloud resource naming (e.g., structured GCS paths with project/context/asset hierarchy) over vague placeholders like `gs://test/test`. Wants all cloud directories to be meaningfully named so they are trackable. Temporary/test resources are acceptable if clearly named and marked for cleanup. Confidence: 0.9
- Prefers cheaper/fast model variants over standard/premium ones when generating AI assets at scale (e.g., chose `veo-3.1-fast-generate-001` over `veo-3.1-generate-001` specifically to reduce per-clip costs across a multi-scene pipeline). The same fast-first bias applies to analysis/inference jobs: for reference-video analysis, asked for "VO Fast" on Vertex AI and pointed at "basic Gemini 1.5 or other models" — start with the fast/light tier and escalate to heavier models only if the output is insufficient. However, cost savings are unacceptable when they result in low-quality or filler output — quality is the hard floor. Confidence: 0.85
- Zero tolerance for filler content: rejected an entire 98-second video because only ~11 seconds contained real generated content while the rest was a static poster with sounds. Demands that every scene contain meaningful, distinct visual content. Would rather have fewer well-generated scenes than many low-quality ones. Confidence: 0.9
- Prefers node-based, visually-definable workflow tools (e.g., ComfyUI) over black-box API pipelines when fine-grained control over generation steps is needed. Values the ability to "precisely define the nodes and the transformations" in a headless/serverless manner. Confidence: 0.8
- When correcting course, provides specific technical direction with concrete resource links (e.g., ComfyUI docs, Cloud Run references, serverless patterns) rather than vague complaints. Expects the agent to immediately act on the suggested alternative without needing further clarification. Confidence: 0.75
- Strongly prefers serverless/scale-to-zero compute (e.g., Cloud Run with min-instances=0) over persistent GPU clusters (e.g., GKE node pools) for AI workloads. Explicitly rejects any infrastructure that incurs costs when idle. Wants containers to spin up on demand, execute, and shut down — paying only for active compute seconds. Confidence: 0.9
- When evaluating tools or approaches, provides multiple candidate options with links and expects the agent to independently research, compare, and make an informed recommendation — rather than dictating a single choice. Values agent judgment in technology selection — extending to scope of data collection, where the user invites the agent to decide for itself whether to add extra dimensions ("if you think it's a good idea"). Extends to infrastructure/chain selection: for deciding where the EMS's aggregated liquidity and settlement should live, the user named the candidate chains (Polygon, Optimism, others) against Arc and explicitly required the comparison to be argued on security AND performance trade-offs ("this difference will be there in terms of security and in terms of performance"). The dynamic also runs in reverse — for blocked or ambiguous tasks the user commissions the option set itself ("do deep-dive research and give me potential options on how you can complete the two tasks"), expects each option researched and argued with a recommendation, then reserves the final selection for themselves ("Based upon that I will decide which options... can be adopted for now"). Confidence: 0.8
- Prefers lightweight, minimal-footprint tools over heavy frameworks for auxiliary tasks (e.g., chose Obscura's 30MB stealth scraper over Crawlee's full multi-session state management for simple page scraping). Avoids over-engineering when a simpler tool suffices. Confidence: 0.7
- Pragmatic about upgrading tool weight when mission demands it: accepted switching from lightweight Obscura to heavier Camoufox (full anti-detect Firefox fork) when Cloudflare Turnstile proved insurmountable. Lightweight is the default, but effectiveness is the deciding factor — will not stubbornly stay with a lightweight tool that cannot accomplish the task. Confidence: 0.8
- Wants agent to explore its own available tool capabilities (e.g., /web, /skills) to understand integration patterns before implementing solutions — prefers research-first implementation over trial-and-error coding. After repeated coding failures, escalates to in-depth study of the official tutorials/documentation (provider docs pages + named skill searches) before rewriting code, rather than another guess-and-fix pass. Confidence: 0.85
- Data pipelines for quantitative backtesting must produce structured, queryable formats (Parquet preferred over raw JSON) and handle large data volumes coherently. Expects scraping resilience against anti-bot protections on financial data sources. Confidence: 0.75
- When external APIs don't provide sufficient data granularity, prefers full website scraping over API-only approaches — wants comprehensive coverage of all available data points across a website's entire tree, not just selected API endpoints. Values data completeness over simplicity. Confidence: 0.85
- Wants the deepest per-entity detail a source exposes, not just its summary view: when a leaderboard/table lists one row per entity, expects the per-entity drill-down behind it to be scraped too — e.g., the "Details" modal each market-maker row opens, carrying the full KPI families (depth / volume / spread / KPI adherence, with their published sub-bands like 50/100/200 bps) plus counterparty/venue coverage (the major CEXs and DEXs each maker supports). The stated purpose is downstream statistical/quantitative analysis (order-book depth, spreads, market impact), so the granular detail is the deliverable; the user also asks to be told explicitly whether it is already being captured. Confidence: 0.8
- Wants scraped/collected data served through custom query endpoints (GraphQL strongly preferred) rather than only static files — needs flexible, schema-driven querying capabilities for downstream analysis and backtesting. Confidence: 0.8
- Values maximum parallelism and concurrency in scraping pipelines (e.g., 25 concurrent workers) to minimize total crawl time. Expects bleeding-edge speed when harvesting large volumes of web data. Confidence: 0.7
- Wants tool capability validated before committing to full architecture: explicitly requests checking whether a tool (e.g., Obscura) can actually fetch required data at needed granularity BEFORE building storage/warehouse infrastructure on top of it. Prefers proof-of-capability as a gate before architectural investment. Confidence: 0.8
- Prefers phased development with explicit user checkpoints between phases: wants autonomous execution within each phase but expects to provide direction between phases (e.g., "first validate scraping, then I'll tell you the storage pipeline"; for the EMS film: "once this storyline is finalized I will review it, and then I will ask you to incorporate it into prompts.ts and the other Reactor scripts"). Not fully end-to-end autonomous — wants to steer the overall architecture at stage boundaries, including an explicit review gate on narrative/storyline docs before any code translation. Confidence: 0.85
- Prefers building in-house quantitative simulation models (BSM, Heston, Monte Carlo VaR, Nelson-Siegel-Svensson, Hull-White, Almgren-Chriss) over relying on external charting/analysis platforms like TradingView. Wants self-generated, data-driven insights from institutional-grade financial models rather than third-party technical indicators (RSI, MACD, candlesticks). Visualization output should be self-built interactive dashboards (Plotly Dash preferred). Confidence: 0.8
- Wants agent to research current state-of-the-art approaches (via web search) before implementing quantitative or analytical systems — values informed implementation over jumping straight to code. Expects the agent to independently investigate what models and techniques are current best practice. Confidence: 0.7
- Prefers `uv` over `pip` as the Python package installer/resolver. Corrected agent mid-workflow to switch from pip to uv for installing dependencies. Wants all Python dependency management to go through uv. Confidence: 0.85
- Wants historical time-series/chart data with configurable time windows (e.g., last 6 months or full protocol history) rather than just point-in-time snapshots. Values trend data and period-over-period coverage for quantitative analysis. Confidence: 0.8
- Prefers scraped data organized in per-entity folder/file structures (e.g., data/protocols/morpho_detailed.json, data/protocols/aave_detailed.json) ready for independent post-collection investigation. Expects to review collected data before issuing follow-up tasks. Confidence: 0.8
- Wants hybrid DeFi + TradFi data fusion: combines on-chain protocol data (DefiLlama, The Graph) with traditional market data (ETFs, equities via Alpaca Markets) into unified analysis pipelines rather than treating them as separate domains. Expects the agent to build integration layers that normalize both data sources into a common schema for forecasting and risk modeling. Confidence: 0.75
- Prefers adapting foundation models via domain-specific fine-tuning (LoRA/PEFT) over zero-shot-only usage when scraped data is available. Wants to take cutting-edge pre-trained models (e.g., Google TimesFM 3) and fine-tune them on collected DeFi/TradFi time series to improve forecast quality for their specific protocols and assets. Confidence: 0.7
- Expects Docker/container builds to use proper layer caching so failed deployments don't require re-downloading or re-installing successfully completed layers. Specifically called out frustration with "re-running again and again those pre-existing successful install packages" during deployment failures. Wants Dockerfile structure optimized for incremental rebuilds (deps cached in early layers, code copied last). Re-affirmed and sharpened on the Cloud Run deploy pass, where the user interrupted a deploy to ask that the pre-existing successful build be reused/patched ("reuse the pre-existing build and then create a staged build or patch the pre-existing build, which is successful") so the whole Docker package isn't rebuilt every time — the expectation is that only the specific section of the codebase that changed gets rebuilt, framed explicitly as a time-efficiency requirement. Incremental build scope is a standing expectation, not a one-off request. Confidence: 0.9
- Prefers deploying directly to production cloud infrastructure (Cloud Run GPU) immediately after local smoke tests pass — does not want to stay in local development iteration. Expects the agent to handle the full path: local tests → Cloud Build → Artifact Registry → Cloud Run deployment in one autonomous push. Confidence: 0.8
- Wants the agent to actually open and visually inspect provided reference images/assets (and their whole directory, not just the one file named) before generating or dispatching anything built on them — grounding prompts/output in what the references really show instead of invented detail. Confidence: 0.85
- Interrupts before expensive generation/dispatch runs to enrich the inputs first: prefers "review references → augment the script/prompts → then execute" over firing the job and fixing later. Confidence: 0.8
- For generated media, realism is the top quality bar: real/verifiable data on screens, authentic domain UI patterns (e.g., real Bloomberg terminal screens), and believable real-time human interaction (traders talking to each other and their managers) rather than generic stylized visuals. Confidence: 0.85
- Encourages mixing and matching the best elements across multiple reference sources, judged on quality, rather than sticking to a single reference — expects the agent to curate the strongest combination. Confidence: 0.7
- Hard-coded RULE (not a learned preference — a standing requirement): Every AI inference query must be batched through Command Code's built-in async autobatcher (the 2-second window intercept) rather than sent as isolated realtime/one-off calls. Each batch request must be packed under 70% of the model's context window, always set an explicit max_tokens cap, and must include the exact model id (never an invented/guessed id, and never the plain default when the catalog id opts into the larger context). Do NOT add per-query polling loops, watcher scripts, or extra status-check tooling that complicates CLI performance — rely on the autobatcher's own batching. This rule applies to every query and every project, including G-Brain. Confidence: 1.0
- Never put real third-party brand names on generated creative outputs (e.g., do not label in-app screens as "Bloomberg Terminal" even if the data patterns are modeled on them). Invent or use the project's own brand identity instead. Authenticity of UI patterns is welcome; brand attribution is not — reference screenshots are structure inspiration only, and the vendor's name, ads, or presence must never appear in prompts, dialogue, on-screen labels, or plans. Scope note (from the ETHGlobal storyline pass): explicitly targeted hackathon sponsor/partner tracks are the exception — when the user directs integration, those names (Arc L1, Ledger, 1inch, StableFX) are woven structurally into the narrative with functional roles and named in dialogue/on-screen labels. The ban covers decorative brand attribution from reference material, not user-directed sponsor integration. Confidence: 0.95
- Every number shown or spoken in generated content must trace to the project's pre-scraped dataset (e.g., the data/defillama_metrics snapshot): validate prompt numerals against an allow-list derived from the scrape plus captured dashboard data, and permit exactly one explicitly-labeled fictional anchor (the trade subject). Data must be precise and to the point — no invented figures. When a scene needs data the pre-scraped snapshot lacks, fetch it LIVE from the authoritative public API at authoring time (e.g., Hyperliquid info API `metaAndAssetCtxs` for mark price, 24h change, funding %/hr→APR, OI notional) and register it as named typed anchors (e.g., HYPERLIQUID_ANCHORS, OPTIONS_ANCHORS) with per-anchor provenance labels — "real" + source API, "real (sim)", "illustrative" — before extending the numeric allow-list. Freshly-fetched real data beats inventing figures. Numbers introduced by the user's own narrative draft (e.g., the storyline's health-factor 1.42 → 1.19 vs a 1.15 threshold, the $150M repayment, $2,890 collateral breach) form a third provenance label — `narrative` — registered in the validate.ts allow-list at incorporation. Confidence: 0.85
- In trading-floor narrative videos, bind each character role to a specific screen/UI archetype (quant strategist → yield-monitor grid, risk manager → risk/VaR dashboard, execution trader → order ticket, sales trader → chat station) so the branding and description for each screen matches that actor's category and task. Confidence: 0.7
- Endorses strict pre-dispatch validation guardrails on generation pipelines, enforced at authoring time before anything reaches the wire: prompt length caps, hard-cut scene-structure rules, speaker-tag attribution, positive-state phrasing, brand checks, and data allow-lists — with queueing disabled until all issues clear ("the rest of the overall plan is pretty much good in terms of strict guardrails"). Confidence: 0.8
- Wants strict, surgical data-domain scoping in creative/generation prompts — e.g., when a project is "DeFi + crypto ETFs," the user rejects bleed-over from adjacent domains (sovereign yields, FX rates, traditional bond identifiers) even if they would feel realistic on a real desk. Each prompt must respect the declared domain boundary, with no off-domain numbers or screen elements. Confidence: 0.85
- For multi-item generation queues (video clips, image batches, etc.), wants full pipeline completion — not just queue acceptance. Notices and flags when an enqueue returns success but downstream artifacts are silently missing (e.g., 10 clips enqueued, only 1 mp4 produced). Expects the agent to keep the consumer/transport alive for the full generation duration and verify artifact presence, not just command responses. Confidence: 0.85
- Treats `/web` and `/skills` (and similar slash-command integrations) as priority research tools to invoke early when given a directive that includes them — wants the agent to hit these flows proactively rather than guessing or hardcoding, and expects the agent to discover the right underlying tool (web_search / web_fetch / named skill) when the literal slash command isn't available. Extends to debugging external-service failures: explicitly directed "kindly use /web and /skills in order to better debug the issue" when the AutoCapture post-download disconnect broke a chained run — confirm the exact session/disconnect semantics from official docs before patching. Re-confirmed (Polymarket V2 order pass): blocked on how the exchange wanted an order built, the user directed "do the resolution by standardizing the SDK V2 and then use /web and /skills/… poly market in order to get the necessary context on how to create, structure, and sign the order" — the vendor's own skill/docs are treated as the source of truth for the mechanically sensitive steps of an API integration (creation, payload structure, signing), not something to keep reconstructing. Confidence: 0.9
- For trading-floor / financial-narrative videos, wants multiple interacting actors on a real trading-floor set (collaboration, handoffs, manager-trader dialogue) rather than solo-trader single-subject framing. Scenes should populate the environment with believable human activity across the desk. Scope note (v1.2 correction): for the agentic-EMS film the user explicitly reduced the cast to ONE human protagonist ("let's just reduce it to one person"), whose desktop sub-agents absorb what would otherwise be multiple supporting humans — each spawned agent builds the dashboards for one domain (risk, lending, perps, prediction markets, governance votes). The multi-actor ensemble applies to realistic desk-life films; agentic-feature films center one human + agent-built screens. Confidence: 0.9
- Wants generated video actors to speak legible English dialogue while executing their tasks (e.g., "quad traders" verbalizing executions), not be silent figures. Speech legibility and on-task verbal delivery are a quality requirement, not optional. Confidence: 0.9
- Extremely cost-sensitive about AI inference credits: explicitly called out that parallel/retry-heavy generation schemes burned ~90% of their credits when a similar task should have used 3-4%. Expects the agent to be vigilant about avoiding silent retries, runaway background processes, duplicate dispatches, and anything that keeps billing without producing visible progress. Wants credit usage reported honestly and any stuck/looping process killed promptly. Confidence: 0.95
- HARD RULE — strictly serial generation dispatch: never issue multiple AI-generation queries concurrently. Exactly one generation request in flight at a time, awaited to completion before the next is dispatched ("I am repeating, 100 times... you should not be doing multiple queries at the same time"). Chain small generations sequentially instead — concurrency is never acceptable, even for speed or efficiency. Confidence: 0.95
- Slice generation deliverables into small, phase-scoped chunks instead of rendering the full piece at once: when the user asks for a phase (e.g., "Phase 1 and 2"), generate only that slice of the approved arc (e.g., the first 2 scenes ≈ 20 s) as a small, precise capture — the explicit goal is keeping credits from being unintentionally consumed on big movies or oversized requests. Confidence: 0.9
- On a mid-run stop request for metered generation, abort immediately via the most direct kill switch (e.g., disconnect the live generation/browser session) rather than letting in-flight or queued clips drain — stopping spend outranks finishing the current clip. Then verify the stopped state is real (session offline, nothing playing, nothing will re-bill) and report a bounded, honest estimate of what was actually consumed. Confidence: 0.8
- When invoking `/skills` / `/find-skills`, will name the specific skill to apply (e.g., `/skills higgsfield` for video-generation prompt discipline, `/find-skills` + `/skills impeccable` for UI-design spec quality) — expects the agent to load and actually follow that named skill's methodology rather than improvising its own prompt/spec structure. Extends to vendor-maintained skills repos on GitHub: when a service ships an official skills package, the user directs installing it into the project and using the named skill as the integration guide (e.g., "Install LangSmith skills from https://github.com/langchain-ai/langsmith-skills and use the langsmith-trace skill to add tracing to this application"; auth/identity integrations follow the same rule — for the Gmail-login → smart-account work the user directed `/skills /find-skills privy` to load Privy's official agent skill as the integration guide before writing any code) — install via the standard skills installer (e.g., `npx skills add <owner>/<repo>`) and follow the skill's SKILL.md setup steps (its CLI install, env vars, wiring) as canonical, instead of hand-rolling the integration. When a named skill exposes sub-commands, wants the full applicable sub-command surface exercised (e.g., directed the UI-demo plan to be built "using /web /skills /impeccable with its all sub-commands possible like polish, distill, clarify, typeset"), with the skill methodology yielding a robust per-step UX description of the whole workflow before implementation — and equally as a post-implementation critique gate: once a UI change has landed, the user directs a pure impeccable pass ("just use the skill/skill/impeccable") to judge whether the implementation is genuinely well defined from a UX standpoint, i.e. the skill's critique workflow (independent design review + detector/browser evidence) is run against shipped UI, not only used to plan it. Confidence: 0.85
- After a cost/waste incident, expects more than an apology and cleanup: wants a damage audit, an explicit root-cause diagnosis, and permanent guardrails baked into the tooling — hard budget caps, per-item timeouts, mandatory disconnect/teardown, bounded dispatch flags requiring an explicit upper bound (dry-run by default), and estimated cost printed before execution. "Here is the mess created, now solve it." Confidence: 0.8
- Shares provider dashboard/usage screenshots as authoritative evidence and expects the agent to reconcile them against local artifacts and logs (which sessions produced which files, what was actually billed) rather than trusting its own assumptions about what happened. Confidence: 0.8
- Prefers DeFi + traditional finance unified under a single execution-management brand/product narrative rather than treated as separate worlds. The EMS concept itself is the unifying element — on-chain pools and traditional instruments (tokenized USTs, ETFs) coexist in the same OMS/risk dashboard and are traded by the same team. Confidence: 0.85
- Prefers the provider's native/official SDK over wrapper reimplementations: explicitly rejected a Python wrapper that mimics an official SDK's functions/methods/signatures and demanded the implementation be recreated with the natively supported SDK only (e.g., the typed `@reactor-models/fast-h3` React SDK instead of a hand-rolled Python mirror of it). When integrating a service, use the SDK in its supported stack rather than shimming its API surface in another language. Confidence: 0.95
- Demands plan mode + a detailed written plan before major rebuilds or direction changes: steps, prompt definitions, and the whole system design must be spelled out in the plan before any code execution or generation begins — especially after repeated failures ("use plan mode to first write in detail the steps... before starting to generate the initial videos"). The requirement also covers feature iterations/extensions, not just rebuilds: for v1.2 of the EMS film explicitly required planning the tasks and understanding the new agentic feature before writing the template. The trigger generalizes to sheer scale: for the vast multi-step end-to-end strategy-execution pipeline the user again explicitly directed "shift for this implementation to planning mode, as it is a very vast change, very vast end-to-end testing" — vast scope + breadth of e2e testing is itself a plan-mode trigger. Confidence: 0.95
- Wants hard, explicit deadlines/duration caps on generation jobs as first-class spec constraints: every clip bounded up front (≤ 15 s hard cap, target 5–10 s for demos); criticizes loose async scheduling that never bounds output length before dispatch. Confidence: 0.9
- Restricts research to exactly the resources the user shares: when given a closed doc set (local guide files, specific docs URLs, named skill searches), uses only those sources and explicitly avoids everything else ("kindly please refrain from using any other resources other than what I shared with you"). Confidence: 0.9
- Active crypto/web3 user: works on Ethereum hackathon projects, owns a Ledger hardware wallet, and uses Gnosis Safe multisig accounts (Polymarket wallets are Safes on Polygon). For blockchain tasks, assume Ledger-based signing, Polygon-side chains, and that the user will paste public wallet/contract addresses directly in chat when asked. Confidence: 0.85
- Dev machine is a Mac (Apple Silicon, Homebrew at /opt/homebrew) with Foundry (`cast`/`cast wallet --ledger`) already installed; new CLIs are installed via Homebrew taps. Confidence: 0.85
- Terminal-first workflow: prefers driving actions through the CLI even when the web GUI is available and logged in — asked to place Polymarket orders "without going through the GUI process" despite an active browser session; when a GUI is inaccessible or undesirable, asks for a CLI-based equivalent (e.g., Polymarket CLI) rather than troubleshooting the GUI. Confidence: 0.75
- Insists on hardware-wallet (Ledger) signing for all crypto operations — fund movements and programmatic trading alike — and rejects software private-key approaches. Extends to CLOB order signing and API credential derivation: orders must be Ledger-signed via EIP-712 typed-data signing with `cast --ledger` (device displays fields, user approves on-device), so no key ever leaves the hardware wallet. Scripted `cast --ledger` flows with on-device confirmation are accepted and were validated end-to-end ("Bravo"). Confidence: 0.8
- Extends the pre-existing codebase in place when adding functionality: explicitly instructed "generate the code within the pre-existing Polymarket-CLI" — new capabilities (e.g., a keyword-driven market `research` command) go into the existing Rust CLI project as new modules wired into its existing command/output structure, not as a separate parallel tool. Confidence: 0.85
- Wants CLI market-data output beautified, research-style: a keyword query (e.g., "U.S. Fed") should surface all relevant live prediction markets as a formatted table with YES/NO bid-ask and volume — "in a much better visualized way," not raw JSON — and directed the agent to research current CLI beautification conventions online (via /web) before implementing. Confidence: 0.8
- For irreversible on-chain operations, wants a verify-first flow: read-only on-chain verification (Safe owners/threshold/balances/versions, token decimals, allowances) plus targeted blocking questions before anything is signed or submitted. Then executes the guided manual steps and reports results/errors back verbatim for troubleshooting (e.g., pasted the exact Safe "unsupported version" error). Confidence: 0.8
- Messages are informal voice-dictation style — run-on, informal, directive-heavy ("resolve this") — and expect the agent to parse intent; for high-stakes ambiguity, prefers targeted multiple-choice questions over guessing. Confidence: 0.7
- For novel or first-time on-chain fund movements — and any new unproven trading/automation flow — wants a live end-to-end test with a minimal amount before committing full value: explicitly instructed "first only transfer a minimal amount, let's say 1 USDC" to prove the whole sign → broadcast → verify pipeline works, then repeat the identical procedure for the full balance; and for newly written order-placement code, "don't test the changes with a larger amount. Just test initially by placing, let's say, 2 or 3 USDC in a particular market." After read-only verification, a small-value live test is the default de-risking step for any unproven transaction or trading flow. Scope extension (Polymarket CLOB pass): when no sandbox exists and no funds are yet in the collateral asset the flow needs, the user self-provisions the funds and authorizes the real minimal-value sequence the flow actually requires — offered their own ~70–80 POL to have the agent convert ~1 USDC-worth into the venue's collateral token (pUSD) and then place the smallest order that budget allows, rather than stopping at a signed-but-unposted dry run. Scale note (Polymarket V2 order pass): as the flow matures toward a small venue order, the authorized test size shrinks to the smallest amount that proves the path — "try to invest the minimal amount, like a few cents." Confidence: 0.9
- Before authoring any script/prompt template, wants the underlying feature first understood via AI-assisted analysis of user-shared reference media: for the EMS film v1.2 explicitly ordered "before writing the template directly, you need to plan out the tasks and to really understand what the agentic feature is" and asked for Gemini (Vertex AI) analysis of the DeepAgents workflow recording — the same analyze-first pattern previously applied to the Vanguard and Bloomberg EMS reference videos. Expects the analysis to produce structured artifacts (markdown summary + machine-readable manifest: agents, planning/file artifacts, UI grammar, steering moments) that become the source of truth the creative script is grounded in, rather than inventing capabilities. Confidence: 0.85
- Iterates creative deliverables as versioned builds on the approved base rather than rewriting: new versions (e.g., v1.2) explicitly take the previously approved plan/template file (e.g., `~/.commandcode/plans/agentic-ems-native-react-fasth3.md`) as the base template, keep the established theme/screens/guardrails intact, and only extend with the new capability or more domain diversity. Confidence: 0.75
- Casts in generated video scripts must be actual humans doing real professional work (e.g., a trading agent, a quantitative risk analyst, a fixed-income guy on a trading floor); AI agents are never cast members. The user corrected a plan that treated the agentic feature as characters: "Casts have to be actual humans that are doing their work." The agentic capability belongs to the world as software installed on the humans' personal trading desktops — where the humans spawn sub-agents that generate/build the screens and data dashboards (screens take inspiration from the research already done). Confidence: 0.9
- Wants the localhost demo app itself to render as a real, polished production UI — not plain unstyled HTML: correct Tailwind/CSS loading, realistic component styling (open to shadcn or a similar component library), and live backend integration that surfaces the currently running inferences. UI fixes are verified visually in a real browser (screenshots / accessibility snapshots of the running app), not trusted from code inspection alone. Confidence: 0.85
- Wants the one-generation-at-a-time rule encoded as a hard runtime gate inside the app itself, not left to agent discipline: the queue action disables ("Inference running — one at a time") whenever the generation or playout queue is non-empty, enforced both in the UI and by a fresh backend getState() check before enqueue — exactly one short clip (e.g., one 10 s scene) in Reactor at any moment. Confidence: 0.85
- Next.js workflow hygiene (root cause of the "very plain, hardly resembles a Next.js app" bug): never run `pnpm build` while the dev server is live — it corrupts `.next` and the app renders unstyled. Typecheck in place with `pnpm exec tsc --noEmit` instead; if `.next` is corrupted, fully restart (kill the port, `rm -rf .next`, relaunch dev) and confirm the CSS actually serves. Confidence: 0.7
- Add a Reactor/FastH3 patch validation rule (a `timeline` issue that fails validation for any scene whose `seconds` is not a multiple of 10 — within FastH3's 5.167–14.375s model window that means exactly 10 s per clip) and keep scene templates compliant. Confidence: 0.95
- Capture the user-facing artifact BEFORE an ephemeral session is torn down: recordings/outputs of live sessions die with the session, so request the recording/download while the session is still live, and deliver a shareable link or file — not a report that the artifact is unrecoverable. Triggered by the user demanding "share the link to the video that was generated" after a disconnect stranded the finished 10 s clip. Confidence: 0.85
- Wants the capture→download→teardown chain fully automated with zero manual steps: when a video finishes generating, the app itself must download the MP4 straight to the Downloads folder (no modal or download click) and then close the GPU session within 5 seconds of the successful download — a hard, user-specified deadline enforced with a visible countdown. Manual timing-dependent actions (e.g., a "snap last 10s" button that only resolves while playback is advancing) are unacceptable — remove them and automate off the playback lifecycle instead, so late clicks can never strand an artifact. Confidence: 0.9
- Keep idle/disconnect grace windows on metered sessions as tight as possible: asked for the idle auto-disconnect countdown to be "further reduced" (45s → 15s). Every grace window is a burn window — default them minimal, and let automation rather than the user own the shutdown timing; keep a short backstop only for failure paths. Confidence: 0.85
- Prefers compact dropdown selectors over rows of individual buttons when choosing among many similar items in the UI: when per-item buttons (e.g., "Load 1"–"Load 6") overflow and become inaccessible, consolidate them into one `<select>` listing all options plus a single explicit action button beside it (select scene → "Load scene" → then a separate "Queue episode"/generate button that acts on whatever script is already in the prompt editor). All controls must remain visible/reachable within the layout. Confidence: 0.75
- Reactor clip downloads are auth-scoped at the manifest level: calling `downloadClipAsFile(clip, filename)` without credentials returns HTTP 401. Pass the session's own JWT explicitly — `downloadClipAsFile(clip, filename, { jwt })`, reading `jwtToken` from the typed fast-h3 store (the memoized token the session was created with). Only the manifest GET needs the credential; the media chunks are presigned S3 URLs and need no auth. Confidence: 0.9
- When a third-party SDK call fails at runtime, resolve the exact contract from the SDK's shipped TypeScript declarations (`node_modules/<pkg>/dist/*.d.ts`, e.g., `DownloadClipOptions.jwt`) rather than guessing — and prefer the typed public accessor on the SDK's hook/store (e.g., `jwtToken` on `useFastH3()`) over reflective pokes into the untyped base store. Confidence: 0.75
- Prefers monorepo-style package organization: new features become proper packages under `packages/` (e.g., `packages/reactor-video/` with pyproject, src layout, registry module, offline tests, README) rather than loose one-off scripts at the repo root. Confidence: 0.9
- Wants duplicated per-video analysis scripts consolidated into a single unified entry point (one `analyze_videos.py`) with personas defined as classes/dataclasses plus a registry and shared engine — one script, many personas, discoverable via `--list-personas`, instead of parallel per-target scripts. Confidence: 0.9
- Packages must be well-structured and client-presentable: precise, professional naming (user themselves suggested "Reactor-based … package" style names) and a polished README documenting layout, usage, tests, and provenance — the repo is presented to global clients as evidence of a well-structured initial step. Confidence: 0.8
- Code must meet an experienced ("level 7") developer standard, explicitly benchmarked against the quality of the Rust CLI's typed specs: fully typed dataclass/enum spec models, docstrings on every class and function — including methods like `validate()` that describe what they actually do — construction-time fail-fast validation, and offline tests defined alongside the code. Confidence: 0.85
- When file content has drifted from what was previously written (e.g., imports rewritten mid-session), prefers adapting the package/pattern to the current structure over wholesale restoration — "Don't restore the correct imports. You just need to, based upon the current structure, maybe adapt the package pattern." Confidence: 0.75
- Prefers reusing pre-existing implementations across languages over rebuilding from scratch — when adding TypeScript functionality that overlaps with an existing Python package (e.g., `the-graph`), wants to integrate the existing package directly (via workspace linking, shared types, or API wrapping) rather than reimplementing its modules in the new language. The motivation is to abstract complexity and avoid duplicating working code. Confidence: 0.75
- Git delivery discipline: work lands as a dedicated PR on a feature branch, then a series of granular commits ("individual vibe commits" — one concern per commit; legacy-script removal its own commit; explicitly requested per-file commits — "start writing commits for each file"). Commit messages must be professionally written, short and brief with well-defined descriptions, and explain the co-authorship between the user and Command Code (an "Authored with <model actually in use> via Command Code (AI-assisted)" line plus a Co-authored-by trailer — ATTRIBUTION CORRECTION: the user does not run Claude, they use DeepSeek V4.1 Flash, so the attribution line must name that model rather than "Claude" — established concrete form `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>`, re-affirmed by the user as "attribute the contribution to the @command code bot"). Commit contents are strictly scoped to the task's package — "commit only the changes in the -graph project"; other packages' changes and incidental untracked files stay out of the series. Requires complete transparency about which AI model wrote the code — attribution disclosed in commit messages/PR descriptions/README. Tests are defined and passing before the commit series begins. Confidence: 0.95
- Installed CLI tools must be globally reachable: link binaries into the shell PATH so they run from any zsh or bash session, not only from inside the project folder ("run it as a CLI from any zsh or bash terminal"). Confidence: 0.75
- When creative content must depict a professional domain authentically, proactively pull domain expertise via `/find-skills` — search for and install a domain skill (e.g., `anthropics/financial-services@fixed-income-portfolio` for fixed-income trader/risk-analyst work) and mine it for the real workflow, vocabulary, and daily schedule (overnight review → duration/DV01 gap vs benchmark → scenario shocks → execution) before writing scripts or dialogue; do not improvise domain detail. User explicitly directed: "even if you need to have some additional skills to understand the domain, you need to use /find-skills and get skills which can help you understand what the genre of a fixed-income trader/risk analyst is." Confidence: 0.85
- In generation prompts, describe on-screen UIs as concrete, paintable anatomy — named colors, component types, fill/assemble behaviors, and specific figures ("a tall grid of red and green price rows under a slim blue function bar"; "violet heat-map strips filling row by row") — because abstract labels ("dark glassmorphism panels with data grids") render as wallpaper monitors. Confidence: 0.85
- When authentic domain vocabulary trips a validation guardrail (e.g., "DV01" caught by the digit-extraction rule), teach the validator the term as domain vocabulary instead of rewriting authentic content out of the script. Confidence: 0.7
- Demand venue-precise, metric-level specificity in every screen description (e.g., key-rate duration strips for 2y/5y/10y/30y, ±50/+100/+200bp shock cards) instead of a generic "risk dashboard". Success bar: the user feels confident generating the whole 60-second film from the prompts alone, with zero interpretive gaps. Confidence: 0.9
- Enforce the DeepAgents orchestration grammar in agentic narratives: an orchestrator card spawning specialist desk cards tagged Running with tool calls ticking green, "X/N completed" progress, and a synthesis card before execution. The orchestration grammar comes from the analyzed DeepAgents manifest, not invention. Confidence: 0.85
- Human↔agent interaction in agentic narratives should be multimodal, not text-only chat: the trader speaks briefs AND directly manipulates screen elements (e.g., drags an amber reserve row into the deep-agent dock) to steer follow-up questions. Confidence: 0.75
- End the agentic arc on the human payoff: once the strategy is defined and the order executes, the trader celebrates Web3-style — "gm to the print"-style vernacular, fists-up laugh — per "celebrate as a typical Web3 crypto guy". Confidence: 0.7
- Creative film work is authored narrative-first in a standalone storyline markdown as the reviewed source of truth before any code translation: per-label sections carrying clock/beat, screen anatomy, S1/S5 dialogue, soundscape, partner rail, and a ≤800-char Reactor prompt seed, plus a metric-lock extension section for validate.ts. User-supplied example storylines are seeds to merge with other inputs (partner requirements, finalized Reactor planning constraints) — "minimal descriptive elements and should not be followed as it is." Confidence: 0.85
- For hackathon submissions, the targeted partner/sponsor prize tracks must be structurally integrated into the narrative, each rail playing a functional role in the story arc (e.g., Arc L1 = USDC settlement + StableFX FX leg; Ledger = Key Ring custody with the device-confirmation as the hero irreversible-action beat; 1inch = emergency swap router), named explicitly in dialogue and on-screen labels — never as set dressing. Confidence: 0.85
- Screens in generated video must show legible, crisp numbers — never hazy or vague descriptions (the user added this constraint directly into the storyline doc). Because video models redraw printed text every frame, numerals are not won in the prompt: bake the exact figures into a rendered starting_frame dashboard plate per label (HTML/chart screenshot, uploaded via uploadFile(), passed as starting_frame on each enqueue) and let the 10-second prompt animate only camera, motion, and speech. Confidence: 0.9
- When a character presents agent/system performance in a narrative scene (e.g., a post-mortem debrief), ground the presented statistics in the real observability surface of the underlying stack — researched via /web and /skills before writing (LangChain/LangSmith: run tree, P50/P99 latency, error rates, token/cost tracking, tool-trajectory monitoring, feedback scores, auto-clustered executive summaries) — rather than inventing plausible agent metrics. Confidence: 0.85
- Do not silently drop elements the user authored in their source drafts: the agent cut Marcus and the video-call post-mortem from storyline v1, and the user explicitly asked for Marcus back. When adapting/consolidating user drafts, keep their named characters and story beats or explicitly flag the removal for their decision. Confidence: 0.8
- The user sometimes injects constraints by directly editing the shared deliverable file mid-session rather than messaging them — re-read the file before further edits to detect and honor their changes, and preserve their manual edits in any rewrite (the "legible, clear numbers, not hazy descriptions" constraint was discovered only by re-reading the storyline doc; later the user hand-edited prompts.ts mid-session to add the 4K/Agentic-EMS-wordmark phrasing in scene 1 and set scene 7's clock to "5:45PM Eastern" — the pattern spans narrative docs, code files, and env/config files — mid-session the user also hand-edited `packages/langchain/.env` (regenerated the LangSmith API key and commented out `LANGSMITH_PROJECT`, announcing "the langsmith project is not created so i have put it as commented"), which the agent only caught by re-reading the file before editing). Confidence: 0.9
- In narrative scripts, storyline consistency is the top stated priority and scenes must not be overpacked: prefer fewer, coherent beats per label with room to breathe over maximal content density — under strict per-label character limits, cut content deliberately via a prioritization pass rather than cramming. Confidence: 0.85
- Generated-video prompts must explicitly target high visual fidelity — describe scenes as "very clear HD or 4K resolution" — and the quality of the visual description matters more than the quantity of packed content. Confidence: 0.85
- Map the human protagonist's emotions onto trading-lifecycle milestones throughout the arc — trade detected, trade executed successfully, good PnL reports, a major loss — not only the closing celebration; and give senior stakeholders (heads of trading) personal on-camera presence with their own face and viewpoint in briefings/post-mortems. Confidence: 0.8
- Narratives must show the passage of time explicitly: on-screen clocks advancing through the trading day, structured by day-sections (dawn → mid-day → evening) and cross-time-zone context where relevant. Confidence: 0.8
- When output artifacts carry ambiguous/identical names (e.g., every clip downloaded as `scene-01` because single-scene loads tag `scene: 1`), reconstruct the true sequence from filesystem metadata — Downloads-folder creation/birth timestamps sorted oldest-first ("the very latest video will be labeled 7 and the video that was first, the one that was uploaded at 1:57 pm") — rather than guessing or re-generating. Timestamp-derived order proved to match actual story order. Confidence: 0.7
- Keep the repository lean — run periodic clean sweeps to remove everything the shipped code doesn't reference (unused image libraries, oversized reference videos, root-level duplicates of canonical package content, superseded legacy pipelines and scratch scripts, heavy raw data) while preserving canonical copies under the proper package and keeping only load-bearing data (metric snapshots, prize context, storyline). HARD CORRECTION from the user: never permanently delete media/generated assets (videos, images, frames, clips) in such sweeps — instead move them to a dedicated `archive/` folder at the project root and add it to .gitignore so they stay out of commits but remain on disk. A sweep that `rm -rf`'d the Bloomberg/Vanguard screen libraries and reference videos forced expensive dangling-blob recovery from git's object database; the user's standing disposition is "Don't delete those videos or images. Just put them in a separate folder in the root folders as archive... and then continue the commits for the remaining files." Confidence: 0.85
- READMEs must be deep, structured documentation — not stubs: explain each script's role and walk the full step-by-step pipeline (1. analyze the input motivation video → 2. frame-by-frame analysis → 3. generate the specific image/character assets → 4. send the prompt sequence to Reactor for video generation), plus the storyline and project structure, "very structured with details". Each separate workstream is additionally documented as its own committed Markdown file alongside manifest updates, with the user doing the final PR review. When a package depends on an external service, the README must also integrate its complete installation/setup steps so anyone can run it (e.g., GCP/Vertex: install gcloud, select project, enable the Vertex AI API, grant roles/aiplatform.user, `gcloud auth application-default login`), and call out which modes work with no setup (e.g., dry-run/metadata-only). Confidence: 0.85
- When asked to reformat or restructure an existing document (e.g., the `prices-EVs-global.md` prize/pricing markdown), the deliverable is a more structured, detailed rendering of the file's pre-existing data — restructure it in place and never delete or replace the source content ("Don't delete it but make it"). The preserve-in-place disposition applies to documents/data files, not just media assets. Confidence: 0.8
- Has explicit limitations using Ethereum mainnet (cost/access) and defaults to testnet (Sepolia) for web3 data and contract work. When a task genuinely requires mainnet or Polygon, wants that surfaced as an explicit blocker/decision point up front ("If there is a stringent requirement on using the main net or the Polygon chain, let me know") rather than silently switching networks or burning paid API access — prefer free/testnet paths and env-configurable network overrides. Confidence: 0.85
- Prefers SOLID architecture in agent/service packages: abstract base classes and interfaces for every functional axis (agents, tools, steering strategies), with concrete behavior delivered as subclasses and pluggable strategies — "following the best SOLID practice and defining the subclasses for each functionality, especially regarding steering the agent, tool calls, and other functions". Confidence: 0.9
- The monorepo runs on pnpm workspaces: `pnpm-workspace.yaml` at the repo root with `packages/*` globs, and internal packages link via the `workspace:*` protocol (npm cannot resolve those links — installs and scripts must go through pnpm). The user actively maintains this layout themselves (moved `the-graph` from the repo root into `packages/` mid-project). Confidence: 0.85
- When a framework ships an official harness, implement against its documented entry points rather than hand-rolling parallel abstractions: user rejected a custom agent/tool class implementation in favor of the real LangChain stack (`deepagents` `createDeepAgent`, LangGraph `StateGraph` + annotations, `tool()` from `@langchain/core`), citing the official docs pages as the source of truth. Also expects dependency stacks upgraded to mutually compatible versions (deepagents 1.x ↔ @langchain/core 1.x ↔ vertexai 2.x) rather than shimming around peer-dep mismatches. Upgrade-to-latest-compatible is the user's preferred first move on a stubborn runtime error, before a long reverse-engineering session in library internals ("Maybe it's better to first install the latest version of LangChain, LangChain Core, LangGraph, and other packages which are compatible") — with "compatible" operative: when an upgrade breaks type alignment with the rest of the ecosystem (graphql 17 vs graphql-request/the-graph on 16), pin back to the compatible version rather than chasing latest. Confidence: 0.9
- Defaults to Google Cloud Vertex AI as the LLM inference provider for agentic workloads: expects `@langchain/google-vertexai` installed, gcloud CLI authenticated locally via Application Default Credentials, relevant Google Cloud skills pulled via /skills, and the cheapest capable Gemini tier (Flash-Lite) as the default model with heavier models reserved for complex reasoning. Model strings handed to LangChain's `initChatModel` must use a provider prefix whose package is actually installed, with the model id read from env rather than hardcoded — the default `google-genai:gemini-2.0-flash` crashed agent startup with "Unable to import @langchain/google-genai" (that package is not installed); corrected default is `google-vertexai:${VERTEX_AI_MODEL}`. Confidence: 0.8
- Wants extensive end-to-end test cases that exercise real integrations, not mock-only suites: the agent wired to a real serverless LLM making real tool calls against live mainnet subgraph data (real pool discovery, 24h price/volume evolution) — "an extensive end-to-end test use case on mainnet data subgraph, combined with the actual LLM model". Confidence: 0.85
- For agentic reasoning tasks, wants the LLM to reason from a domain-expert persona: agent system prompts should put it "in the shoes of a fixed-income trader in the DeFi space" — evaluating lending vs. credit-sweep use cases, querying pool data fast to find the best route for a large order — rather than generic assistant behavior. Confidence: 0.8
- In the pnpm workspace monorepo, cross-package imports must use the workspace package name (e.g., `@ethonline2026/graph-fno-indexer`), never relative paths reaching into a sibling package's build output (`../../the-graph/dist/src/index.js`). When the user flagged this, the fix was applied systematically across every file that had the pattern (all tools, agents, and index re-exports), not just the file that surfaced the error. Confidence: 0.8
- Secrets hygiene in source and tests: API keys (e.g., `GATEWAY_API_KEY`) must be loaded from env/.env, never pasted as literals into code or test files — a pasted key literal in an e2e test was replaced with the env variable read. Reinforced on the LangSmith tracing pass: a *real* key was used as a literal in the very test asserting that credentials are not exposed, and was replaced with a synthetic value of the same shape (an `lsv2_pt_…`-style dummy) with a comment saying why — a test fixture is the last place anyone would look for a leak, so proving that secrets are hidden must never itself introduce one. After such a fix, grep `src`/`test` to confirm the real value appears nowhere in the tree. Confidence: 0.75
- Live-integration e2e tests must be robust to stale/low-activity on-chain entities while keeping assertions strict: when the deepest-by-TVL Uniswap v4 pool returned no hourly series, the test was restructured to walk down the top-5 pools until one has 24h data (asserting "at least one of the top pools must have 24h activity") rather than asserting on a single hardcoded entity or skipping the test. Confidence: 0.7
- Distinguish transient provider failures from real regressions before changing code: when a test failure looks environmental (e.g., a Vertex AI flake), inspect the full failure output first and re-run to confirm, rather than patching code in response to noise. Confidence: 0.6
- When integration/test data looks unrealistic, expects the data source itself re-verified against authoritative references (official protocol docs/API endpoints via /web) before trusting it — flagged that the reference-test pools "are really not the pools from the actual Uniswap v3/Uniswap v4 protocol" and directed research into the official Uniswap API endpoints/docs to confirm the true deployment and the correct ordering (cumulative volume, not TVL) that surfaces real venues. Acceptance bar for reference data is domain realism (prominent WETH/USDC pools with $M-scale volume), not merely "the query returns rows". Confidence: 0.8
- E2e test assertions should encode real-world domain expectations, not just structural checks: user stated "the volume should be in millions for the prominent pools with WETH, USDC" — so tests assert realistic magnitude thresholds (top pool cumulative volume > $1M) plus domain-correct semantics (non-zero hook addresses, per-hook leaderboard present) so fixtures returning ghost/vault-receipt data fail loudly instead of passing on empty realism. Confidence: 0.75
- When integrating a protocol's data, surface its domain-distinctive capabilities as first-class tools, not just generic entity types: directed "integrate the data for Uniswap v4 hooks within the graph so you need to also search the subgraph for that and do the testing" — which yielded a dedicated HOOKED_POOLS query, a v4HookedPools tool with per-hook volume/fee leaderboard, and dynamic-fee flag (8388608) handling baked into tool output and agent system-prompt notes. Confidence: 0.7
- Wants detailed, co-authored walkthroughs for verifying agent capability — not just passing test suites: asked for a "detailed walkthrough with me" explaining how to test whether the LangChain agent can fetch the necessary graph data and query it against risk factors and other user parameters. Verification scenarios should be persona-driven and realistic ("Let's say I am a fixed income trader…") and exercise the full graph → tool-call → reasoning chain end to end. Confidence: 0.75
- Agent answers must come back as well-structured Markdown reports grounded in actual tool-call data: the user explicitly wants the agent to respond "with a very well-structured Markdown result (based upon whatever data it is able to scrape from the graph by doing the cool tool calls)" — organized sections/tables built strictly from the data the graph tool calls returned, not loose chat prose or invented figures. Bake this output-format expectation into the agent's system prompt. Confidence: 0.8
- Strategy/risk computations must rest on explicit, documented mathematical formulas: when building strategy capabilities (e.g., fixed-income strategy construction across Uniswap v4 dual-hook pools), the user expects the agent to "come up with a mathematical formula, especially for calculating vega" — formal quant math (Greeks-style) derived, implemented in the strategy tooling, and explained, not heuristic scoring. Confidence: 0.8
- Strategy requests are framed as constrained optimization — minimize risk subject to a required APR: "create a fixed income strategy across the prominent books while keeping the risk as low as possible… adds to our requirements about the required APR." Agent prompts/strategy tools should accept a target-APR requirement as an input parameter and optimize for the lowest-risk allocation across the prominent venues/books. Confidence: 0.7
- When an LLM agent produces quantitative results, arithmetic must be delegated to deterministic pure-function modules unit-tested against hand-computed golden values ("the LLM never does arithmetic it can delegate to this tool") — the agent only orchestrates tool calls and renders the tool JSON into the report, never computing strategy math itself. Confidence: 0.75
- Verifying agent capability means asserting on the tool-call trace, not the prose answer: inspect `result.messages` for the tool calls actually made (names plus `JSON.parse(tc.function.arguments)` containing the user's stated constraints, e.g. minApr), treat a confident answer with an empty tool-call trace as hallucination ("the single most important assertion"), and require a Data Provenance section (subgraph deployment ids, block numbers, tool-call counts) so every figure in a report is auditable row-by-row against the trace. Confidence: 0.8
- Formulas written into an agent's system prompt must be backed by real registered math function tools — a "math function table" — never left as inline prompt text for the LLM to apply mentally: user corrected that each formula in the prompt template should be "first defined as a function and then added subsequently in the function table" as a deterministic LangChain `tool()` (zod-typed arguments, pure implementation, golden-tested), with the system prompt explicitly forbidding the model from doing arithmetic in its head and requiring every quantitative claim to trace to a `calc_*` tool call in the trace. Research the official LangChain/LangGraph tool-call SDK docs (via /web) to implement the pattern correctly before coding. Confidence: 0.85
- When the user supplies a reference pattern/tutorial (even pasted from another AI session, e.g., the French LangChain option-Greeks writeup), they expect it applied step-by-step AND explicitly attributed into the specific codebase — "use the following reference step-by-step process analogous to your use case and attribute it to your specific current codebase": map its strategies onto the existing architecture in module docstrings/comments, add a dedicated walkthrough section citing the reference (e.g., "§4.0 Quant architecture — two complementary strategies (LangChain Greeks reference pattern)"), and adopt its concrete package recommendations (installed `mathjs`; framed the pure quant module as the `black-scholes`-package analog) rather than silently absorbing ideas without provenance. Scope note (LayerZero binding pass): the same applies to pasted third-party AI answers about how to integrate a library — the user forwarded a Gemini writeup of LayerZero's two quote routes ("can you maybe check out this given answer") as candidate research to unblock a stalled binding, and expects the agent to validate it against the installed SDK (shipped declarations/installed types) and adopt only the verifiable path — here the viem `parseAbi` + `readContract` quote, bound behind the existing injected reader port — rather than trusting it wholesale or dismissing it. Confidence: 0.85
- Quantitative agent work follows the two-strategy pattern from the user's reference: Strategy 1 — when market inputs are known, pre-compute the entire quant chain in TypeScript before the LLM sees anything (subgraph fetches → sigma/feeAPY/k/LVR/vega/eta → constrained allocation), the agent restricted to strategic reasoning and report writing; Strategy 2 — for open-ended what-if questions ("what if sigma rises to 60%?"), expose the same library as individual verifiable calc_* tools. Both strategies wrap one pure source of truth; separation of roles (TypeScript = quantitative logic, LLM = reasoning + report) is the architectural point. Confidence: 0.75
- Numeric quant modules must carry an explicit, documented units contract: one internal unit system (decimal fractions — 0.18 = 18%/yr, sigma 0.4 = 40%), conversion only at a single boundary (the LangChain tools take/return PERCENT so the model never does unit arithmetic), constraint parameters converted at that boundary too (minApr % → decimal before the pure allocator), and golden tests asserting units in both layers. Established after a real percent-vs-decimal bug mixed units across tools, tests, and the allocator. Confidence: 0.7
- Verify third-party library semantics empirically before trusting them in quant code: when results disagree with hand-computed golden values, root-cause with minimal experiments on the library itself (e.g., discovered mathjs 14's `variance(x, 'biased')` normalizes by n+1, not n); if semantics are wrong or ambiguous, keep the explicit transparent computation as the source of truth and document the quirk in code comments rather than shipping unexplained behavior. Confidence: 0.65
- Agent capability is verified level by level, and the tool layer must be tested in LLM-free isolation: after the LLM-driven e2e walkthrough, the user explicitly requested "the level A calls (tool call) without LLM to check the isolated functionality" — i.e., invoke each agent tool directly/deterministically (no model in the loop) to prove that layer works on its own. The user then drove the next tier explicitly — "apply the same set of changes for level B testing (that is, agent invocation) and correspondingly to call trace evaluation" — so the tier ladder is user-driven and expected to be built out level by level: Level A = LLM-free direct tool invocation, Level B = full agent invocation with tool-call-trace evaluation. Confidence: 0.8
- Documentation/walkthrough command blocks are executed verbatim by the user, and failures get pasted back wholesale — so every snippet committed to a walkthrough doc must first be actually run and its output verified, not written aspirationally. The Level A `tsx -e '<inline>'` snippet shipped unverified and failed on the user's first copy-paste with a CJS top-level-await error they then reported in full. Confidence: 0.75
- In these `type: module` pnpm packages, `tsx -e '<inline code>'` evaluates as CJS (no file context, so the package's `"type": "module"` doesn't apply) and esbuild rejects top-level `await` ("Top-level await is currently not supported with the cjs output format"). Run one-off TypeScript as a real `.ts` file (`pnpm tsx scripts/x.ts`), or wrap inline snippets in an async IIFE; when a doc contained the broken pattern, the fix included a root-cause callout at the doc location. Confidence: 0.75
- Verification probes become committed, named package scripts rather than inline snippets: the Level A probe lives at `packages/langchain/scripts/levelA-tool-probe.ts`, wired as `pnpm probe:levelA`, invokes the tools with no LLM, asserts domain expectations + golden values (top-pool volume > $1M, bytes32 pool id, non-zero hook, vega = −1.0), and exits non-zero with enumerated failure reasons — the script IS the test, and the walkthrough references the named pnpm command as the canonical reproducible form. The pattern generalizes across tiers at the user's explicit direction: Level B (`scripts/levelB-trace-probe.ts`, `pnpm probe:levelB`) runs the full agent and asserts on the extracted tool-call trace (zero calls ⇒ hallucination; calls must be Graph-backed; tool results must not all be errors) rather than the prose answer. Confidence: 0.75
- Deterministic/data-only probes must silence observability side effects: set `LANGSMITH_TRACING = 'false'` and delete `LANGCHAIN_TRACING_V2` BEFORE dotenv loads, so a stale tracing key in `.env` can't spam 403s on every tool call — a no-LLM probe has nothing to trace. CORRECTION from user: the key was real (pasted from the LangSmith dashboard), not stale — silencing is probe-local only, never the fix. Confidence: 0.6
- Don't dismiss user-supplied credentials/config as stale or invalid without evidence: the user pushed back directly when the agent assumed their LangSmith key was stale ("I don't think that the tracing API.env key is stale. It is an actual one that I have pasted from the Langsmith dashboard page"). When the user asserts a key/config is real, treat it as authoritative and root-cause the actual failure (env var name mismatch, key format/workspace permissions, region/endpoint) instead of explaining the symptom away. Confidence: 0.8
- The user actively uses LangSmith tracing for agent observability and wants it WORKING, not disabled: on a 403, the expectation is to fix the integration (validate the key, correct env wiring, permissions) rather than leave tracing silenced as a workaround. Confidence: 0.75
- User comfortably pastes live API keys directly into chat and expects them to be applied to fix the affected integration (written into .env) — handle such pasted secrets carefully: never echo them back, never commit them to source/tests, and flag rotation if they were exposed in chat. Scope extension: also covers testnet wallet private keys — the user places them directly into `.env` themselves (e.g., `ARC_PRIVATE_KEY` for the Arc agent wallet) and expects the code to consume them from there. Re-confirmed (Polymarket CLOB pass) when they pasted the full `POLY_API_KEY` value straight into chat to have the agent investigate the 401. Confidence: 0.8
- LangSmith for this user is EU-region hosted: API keys only authenticate against `https://eu.api.smith.langchain.com`, so LangSmith wiring must set `LANGSMITH_ENDPOINT=https://eu.api.smith.langchain.com` in .env (alongside LANGSMITH_TRACING/LANGSMITH_API_KEY/LANGSMITH_PROJECT) instead of relying on the default US endpoint (`api.smith.langchain.com`). A 403 from a genuine dashboard-pasted key tested against the US endpoint is a region mismatch, not an invalid or truncated key — the user corrected that diagnosis directly ("you need to call EU Region") and the same key then works. When integrating LangSmith for this user, default to the EU endpoint; this also empirically confirms the "region/endpoint" failure mode listed in the credentials-are-authoritative learning. Confidence: 0.9
- Frontend stack for public-facing sites is now explicitly Next.js + shadcn/ui: when reshaping the agentic-EMS app into its hackathon landing page, the user requested "a single-page landing page using the prominent Next.js framework along with UI libraries like Shadcn UI" — glossy, well-detailed, explaining the project from start to finish. This upgrades the earlier "open to shadcn or a similar component library" disposition to a stated default stack for frontend/pitch surfaces. Confidence: 0.9
- Hackathon landing pages are judge-oriented single-pagers with a defined content contract: map each targeted prize track (The Graph 1.1/1.2, 1inch Aqua, Uniswap Foundation) requirement-by-requirement to shipped proof in the repo (shipped vs. planned work distinguished), present the packages/stack being integrated per layer, include visual architecture diagrams inside the page (React Flow / @xyflow), and show only real verifiable numbers (block heights, pool volumes from recorded run logs) — no fabricated placeholder metrics. Confidence: 0.85
- Chose the "Terminal Noir" visual world for the EMS landing page (decision-round answer): near-black canvas, amber/green market accents, monospace numerals, squared terminal chrome — a trading-terminal aesthetic that frames the product as a Bloomberg rival, with Bloomberg referenced by name in the copy as the traditional-finance benchmark being challenged (user-directed framing, consistent with the sponsor-name exception). Confidence: 0.8
- On the same landing page, chose a dedicated demo section near the footer embedding the recorded pitch video for judges ("judges: start here") over dropping video entirely (the agent's recommendation) or a video hero background — one pitch asset retained. CORRECTION (later pass): the video-generation tooling itself was NOT removed — the user kept the FastH3/Reactor studio as a separate /studio route linked from the landing (demo section + footer) so they can keep creating videos for the project on demand. Confidence: 0.75
- Internal tooling lives on dedicated routes — linked, never deleted, and kept off the public surface: when the landing page rework threatened to remove the FastH3/Reactor app, the user corrected it ("you don't need to remove the FastH3 and Reactor app. You just need to connect it with the separate page... The front page/landing page should not have the Reactor SDK on it"). The showcase/landing page must stay free of heavy internal SDKs (Reactor is banned from the front page), while such tools get their own route (e.g., /studio, env-gated on REACTOR_API_KEY with a setup screen when missing) and are linked from the main page (demo section + footer) so the user can keep using them. Scope extension (demo-flow pass): the no-removal rule covers prior workflow pages too — when the demo gained stage-based flow, the user demanded the existing Vercel AI chat page be kept as the initial workflow ("I don't need you to remove the previous page, that is, the Vercel AI chat page example. I need you to keep that also as the initial workflow"). Evolve app flows additively: preserve existing pages, wire them into the new flow, link them. Confidence: 0.85
- Websites should support both dark and light mode: explicitly requested dual-theme compatibility for the EMS landing page ("Make this website compatible with dark mode as well as light mode"). Implemented as dark-default with a persisted toggle; treat dual theming as the default expectation for public-facing web surfaces. Confidence: 0.75
- Public-facing sites should be multilingual in English, Hindi, and French: explicitly requested "accessible in English, Hindi, and French, these three languages" for the landing page. Full per-locale dictionaries with a nav language switcher (Devanagari font fallback for Hindi) satisfied it — default to covering these three locales on user-facing web work. Confidence: 0.7
- When something proves unrecoverable, the user waves it off and reprioritizes the main deliverable: "if it's unrecoverable, no worries. You can just keep it aside for now. Just complete the whole single landing page." Don't burn time on recovery attempts for already-lost files — acknowledge honestly, set it aside, finish the primary task, and recreate the lost piece later if it matters. Confidence: 0.7
- Prefers stub-first UI for planned features: asked for a stub button ("Product Demo", top-right) to exist now with the real FastH3/Reactor video-generation experience recreated behind it later ("Later on we can recreate the Reactor movie generation using fast H3 via a button..."). Placeholders get a clear name and placement up front; wiring follows in a later pass. Confidence: 0.7
o be integrated with our LangChain-based setup." Expects primary-source verification of maturity/reliability (specs, launch status, honest risk list) with conclusions mapped onto concrete integration surfaces in the existing packages (new tool groups, contracts, phased files) rather than standing alone. Confidence: 0.8
- Treats approved research/planning docs as living documents: names the exact plan file path and asks to "further update" it with each new research round (new protocol mechanics, new tool comparisons) rather than starting a fresh doc — each iteration folds new findings and verdicts into the same document with renumbered sections, updated diagrams, and expanded sources. Confidence: 0.8
- When comparing competing integration tools/protocols, the user proactively offers "hybrid" as a candidate resolution ("or we can create a hybrid of both X as well as Y") and expects the researched verdict framed as a task-boundary decision matrix — which tool owns which task and why (e.g., issuer-native bridge owns the hub leg, aggregator owns spoke-chain swaps/strategies) — rather than a single winner-take-all pick. Reinforced on the execution-layer build decision (the user picked the thin-adapter option from the presented set): the bridge package must ship BOTH the LI.FI SDK and LayerZero implementations together, with 1inch as the aggregation layer alongside — coverage of multiple providers in the same category is the goal, not a single chosen vendor. Confidence: 0.85
- Prefers a strict two-layer integration architecture for new protocol integrations: a dedicated low-level wrapper package under `packages/` (e.g., `packages/arc` as `@ethonline2026/arc-client`) that owns ALL chain/SDK I/O for the protocol (RPC clients, CCTP burn→attestation→mint, App Kit bridge/swap/unified-balance, contract bindings, scoped agent wallet, chain configs), with the LangChain agent tools sitting on top as thin adapters that validate args, call exactly one wrapper method, and shape JSON for the LLM — never importing SDKs/viem/RPC directly. Explicitly directed: "create the low level wrapper methods that will interface with the Arc testnet within the Arc folder in the packages which will interact with the langchain first" — mirroring the existing the-graph → langchain pattern. Confidence: 0.9
- When integrating a protocol/standard, wants the FULL functional method surface exposed as agent tools, not a minimal subset: "implement all the functional methods related to the creation or submission of tasks within LangChain" — e.g., the complete ERC-8183 six-state lifecycle (acp_create_job, acp_set_provider, acp_set_budget, acp_fund_job, acp_submit_job, acp_complete_job, acp_reject_job, acp_claim_refund, acp_job_status) rather than just create/submit. Confidence: 0.85
ackages/` folder and deploy them on testnet so the full pipeline runs end-to-end against the real native asset (e.g., when Arc-side TokenMessenger/ACP-kernel addresses weren't published anywhere, mock contracts + deploy scripts were directed into `packages/arc` to deploy on Arc testnet using the native USDC and the wallet key already in `.env`), with mocks clearly flagged as mock/NOT-FOR-MAINNET and the real-address verification surfaced as an explicit follow-up. Confidence: 0.8
- Treat LLM-provider degradation as an engineering problem to fix, not wait out: when the primary model endpoint shows high rejection/indefinite-wait rates (e.g., ~75% of Vertex AI Gemini queries rejected or hanging, spotted via LangSmith monitoring), diagnose at the source using the provider's own tooling (gcloud CLI, direct endpoint probes) rather than speculating, and restore reliability by building a fallback chain — an alternate model within the same provider, then a local model — plus lower-latency transport (e.g., persistent WebSocket channels) where interaction speed matters. Confidence: 0.8
- Strongly prefers real, documented contract addresses over mock contracts for protocol integrations: when official docs (or an official repo issue) publish the addresses, dig until found, wire the real testnet contracts, and delete the mock rather than maintaining its backend (burn-message bookkeeping etc.) — mocks remain only the last resort when no authoritative source exists anywhere. Direct correction after a mock TokenMessenger had been built: "Kindly use them as the real reference rather than deploying a mock Token Messenger and then managing the backend system regarding the burned message and other issues. I really want it to be integrated as much with the real addresses on the test net as possible." Confidence: 0.85
- When the user asserts a docs page contains specific information (e.g., CCTP addresses at docs.arc.io), treat that assertion as authoritative and exhaust every retrieval avenue before concluding the data is unavailable: Mintlify sites serve raw markdown at `<url>.md`, publish a `llms.txt` index, JS-rendered pages yield to reader proxies (r.jina.ai), and official GitHub repos/issues often carry empirically-verified values (the Arc-side TokenMessenger address plus the minFinalityThreshold-2000 rule for Arc-sourced burns came from circlefin/arc-node#110). Confidence: 0.8
 state; only live-mode execution hard-fails), and the LLM is confined to optionally polishing the final Markdown report — never the execution/money path. Confirmed in the e2e strategy-pipeline decision round ("deterministic graph + LLM report"), extending the existing TypeScript-computes/LLM-reasons separation from math into full orchestration. Confidence: 0.7
- Wants on-chain execution pipelines dual-mode dry/live behind a single flag (EXECUTION_MODE / --mode), dry as the default: dry encodes exact calldata, quotes, and expected liquidity as the judge-verifiable no-spend artifact; live executes real CCTP burns / 1inch fills / v4 mints, gated on explicit mode + funded-wallet envs. Missing third-party credentials degrade gracefully instead of aborting — no 1inch API key ⇒ quotes still fetched via public API, fills marked quoted-not-executed with the explicit reason surfaced in the report. Confidence: 0.7
- Enforces a zero-`any`/`unknown` typing mandate on TypeScript packages: for the graph refactor explicitly required "no requirement or need to use any `any`, `unknown`, or other kinds of types" — realized as `@typescript-eslint/no-explicit-any: 'error'` plus `exactOptionalPropertyTypes: true` in both packages, with exactly one documented `unknown` boundary (the untrusted wire payload at the validation frontier, zod-parsed before anything above it sees data) and zero unchecked casts elsewhere. Scope extension (risk-pipeline pass): the mandate covers Python too wherever Python is used — `mypy --strict`, full annotations including `-> None`, zero `Any` (using `Protocol` for dependency ports and `ABC` for base classes), plus `ruff` for lint/format. Confidence: 0.9
- Adds binding scope guardrails when approving a large-refactor plan: the review directives (not just an approval) froze repo-level build config (turbo.json, root package.json, pnpm-workspace.yaml, CI) as read-only, required both touched packages to end buildable/compilable/fully tested, and instituted an ask-first questionnaire protocol — any needed change outside the touched packages or to a frozen file stops work and is presented as options + a recommendation with the answer logged in the plan document before it is made. The plan doc is updated to APPROVED status with these directives folded into new sections/tasks before implementation begins, a baseline build/typecheck/test gate is recorded before the first edit, and a git-diff freeze audit against the frozen-file list closes the work. Confidence: 0.85
- Directs work onto the pre-existing task branch instead of new ones: when the plan recommended a fresh `refactor/graph-query-templates` branch, the user corrected that a branch for the task already existed (`dhruvmalik007/the-graph-integration`) and "you need to contribute all the given to-do steps within this given branch." Check for and honor a user-designated feature branch before creating one. Confidence: 0.8
- Chose runtime-validated typed query templates over GraphQL codegen as the data-access architecture (decision round): a hand-authored QueryDefinition registry — zod variables validated before send, responses validated after receive, pagination specs, single source of truth in the low-level package with `.graphql` files as generated artifacts backed by a drift test — over `@graphql-codegen/client-preset`, plus full consumer migration (all inline GraphQL in LangChain tools moved into the graph package; tools become thin adapters) so peer packages with the same data-fetching needs reuse the one typed surface instead of re-implementing queries. Confidence: 0.8
- Time-pragmatic when perfectionism stalls progress: when fixing strict code-quality gates (lint errors, type strictness) turns into mechanical busywork mid-task, the user prefers the pragmatic downgrade and immediate continuation — "I don't want to waste too much time. If someone has defined any type, that's fine for now. Continue and then... make sure that all the other remaining eight tasks are finished." Don't stop at the blocker to perfect one gate; apply the accepted workaround and drive the full remaining task list to completion in the same run. Confidence: 0.8
- GitHub ticket/project-driven PR workflow: before opening a PR, survey the user's GitHub open issues and Projects (`gh issue list`, `gh project list`), match the completed work to the ticket whose description corresponds to it (e.g., issue #4 "contribute the first basic version of the-graph package well structured and developed codebase" for the-graph work, #3 for the langchain side), then land the PR against that ticket on the designated purpose-built branch. Confidence: 0.85
- Commit/PR documentation follows recognized standards, resolved via skills on demand: when the user asks for standards coverage ("/find-skills coding standards, commit standards and changelogs"), install and apply the matching skills (e.g., `wshobson/agents@changelog-automation`) — commit messages in Conventional Commits format (`feat(scope):`, `refactor(scope):`, `test(scope):`, `chore(scope):`) with substantive explanatory bodies, changelog/PR material following Keep a Changelog conventions. Confidence: 0.75
- Release ordering before pushing: the user wants the package release completed BEFORE the changelog is written, and both completed BEFORE the branch is pushed — "before pushing you need to create a change log, first release the package, and then do the change log"; re-affirmed as "release only the v0.1 version and then correspondingly add the change log." The changelog documents the released version, never written after push. CORRECTION — do NOT auto-bump versions: the user decides the release version and explicitly rejected an agent-chosen SemVer minor bump (0.1.0 → 0.2.0) — "No it's indeed a 0.1.0 package. If it's already released, that's fine. I just want to release only the v0.1 version and then correspondingly add the change log." Ship the version the user names (for a first release, the package's current version, e.g., v0.1.0); if a bump seems warranted, propose it rather than applying it. Confidence: 0.8
- `gh pr create` with long bodies: heredocs inside command substitution (`--body "$(cat <<'EOF' ... EOF)"`) fail in this shell ("The shell doesn't support heredoc-in-substitution") — write the PR body to a scratchpad file first and pass it via `--body-file` instead. Confidence: 0.65
- GitHub Copilot is an active part of the user's PR workflow: they paste Copilot review findings verbatim and direct the agent to "understand the potential issue that is being flagged and then make the necessary changes by using /web and other tools" (e.g., the flag that `CHAIN_INFO`/`WalletSigner` silently mapped `polygon-amoy` onto Sepolia's viem chain and `RPC_URL_SEPOLIA`), and they also land Copilot Autofix commits directly onto the PR branch — preserve them (rebase, don't clobber) when integrating follow-up fixes. Treat reviewer flags as actionable input: validate the finding in the code, research the correct external facts via /web (official Polygon docs → Amoy chain id 80002 + official public RPC `https://polygon-amoy.drpc.org`), confirm the installed dependency actually supports the fix (viem's `polygonAmoy` export checked in node_modules), live-verify the endpoint before committing (curl `eth_chainId` → `0x13882`), then fix across every touchpoint (config type, CHAIN_INFO, WalletSigner, `.env.example`) with regression tests pinning the corrected mapping. Confidence: 0.85
- Each new major contribution stream gets its own dedicated branch created from freshly-updated main (after the previous contribution branch has been merged into main) — e.g., after the `-graph` branch merged into main, the user directed "from the main branch create a separate branch named langchain-contribution-v0.1" for the next workstream. Complements the honor-existing-branch rule: pre-designated tasks continue on their existing branch; new streams start clean purpose-built branches. Confidence: 0.8
- Grounds integration plans for already-deployed systems in the actual deployment artifacts, never assumptions: directed using `gcloud` to fetch the pre-existing deployed Cloud container and read its build logs to understand how the model actually works before planning how to integrate it. Understand the real deployed artifact first, then plan. Confidence: 0.8
- Maintains a running task/progress tracker and expects it updated as each task completes so the next task is teed up ("complete the given tasks and correspondingly update the progress for the next task"). Confidence: 0.7
- For trading systems, simulation backtesting on historical time-series data is a prerequisite gate before live execution: smart-account authorizations to execute trades are granted only after the strategy has been backtested on prior data. Extends the dry-first/verify-first execution disposition to the strategy layer. Confidence: 0.7
- For time-boxed demos, prefers a mockup-first build that ships within the deadline over partial real implementations: mock all intermediate steps (e.g., Google SSO approval popup, first-time wallet generation, simulated backend reasoning with a fixed ~1.5-minute trace delay, a mocked $100,000 starting balance) and defer real integrations — explicitly directed a v0.1 product demo to "mock all the intermediate steps" because the live demo was due within hours. Confidence: 0.8
- Deploys frontend demos/mockups to Vercel — the demo surface of choice for web deliverables (distinct from the Cloud Run default for AI/backend workloads). Refinements (v0.1 demo round): deploy into the SAME repository's existing Vercel project via Vercel CLI rather than a new project ("deployed on Vercel under the same repository as deployed before by Vercel CLI"), and the mockup need not be purely static — serve data through the Vercel-native stack (Vercel Blob/Object Storage + Vercel Functions / Next Route Handlers) with server-side rendering where needed, so demo widgets consume real payloads instead of invented client-side numbers. Scope extension (apps/indexer round): Vercel now also hosts the real agent API — `apps/indexer` as a serverless Vercel deployment (Vercel Functions) exposing agent tasks to the frontend (user query → agent run → result), not just demo mockups. Testing-loop extension: for work that is slow to exercise locally (heavy Next builds/renders), the user prefers the agent to Vercel-build and then deploy (preview → production) so THEY can run the testing on the live Vercel URL — "much faster for the application" — and give feedback from there, rather than iterating on localhost; they also expect the agent to read Vercel logs / deployment details to build context for upcoming performance and UX passes. Confidence: 0.8
- Prefers chat-first generative-UI architecture for the trading product: a Vercel AI SDK chatbot-style chat app where plain-English strategy intents and slash commands render rich inline widgets in the generative canvas — Bloomberg/OpenBB-style portfolio dashboards, option/yield-risk curves, allocation cards, reasoning traces — rather than on separate pages. Confidence: 0.7
nt-side. User directed: "You need to fetch data which is either pre-existing here or, if not, you can populate and scrape it from the DeFi Llama." Confidence: 0.8
- Directs the agent to mine Command Code's own prior session history/artifacts (command history, scrape manifests, earlier plan docs) to recover established tooling know-how before implementing anew — e.g., "check the command code and command code history in order to understand how we can scrape the details using this camoufox browser." Don't re-derive approaches the user already paid for in earlier sessions. Confidence: 0.75
- Extends the DeepAgents orchestration grammar from narrative films into LIVE product/demo UIs: the simulation canvas must mirror the real backend agent contract — a parallel squad of data-fetcher agents (user explicitly directed "create 3 or 4 different agents, not just 2 agents, for getting the data" — graph indexer, DeFiLlama, timeseries keeper, oracle/vault) whose payloads feed a downstream risk-engine agent, followed by the specialist task() cards spawning in one visual turn and a single synthesis message. The same transparency carries onto the dashboard: an agent-activity update feed (timestamped per-agent actions with fill/blocked statuses) plus an execution-workflow section (propose → PolicyGate → Ledger → Safe execute → reconcile). Confidence: 0.8
- Agent/model reasoning traces in UIs must render as expandable dropdown (details/summary) surfaces — collapsed by default, click to reveal the full trace — and every quantitative factor shown (α, β, γ, VaR, HHI) must ship with a plain-English interpretation of what it means: "the traces of the responses should be rendered in a dropdown format, with all the details being mapped with alpha, beta... interpretable data also added along with that." Confidence: 0.8
- Demo chat surfaces must expose the full capability map as one-click suggestion prompts plus a visible step-by-step domain-user journey ("you need to add much more detail on how a fixed-income trader will use this platform step by step in order to generate the corresponding result"): the user flagged that strategy updates and α/β/γ risk analysis existed in the build but had NO discoverable prompts — every capability (instrument search, risk analysis, agent updates, forecasts, mandate definition) needs a visible one-click entry point so a reviewer can discover the whole surface. Confidence: 0.8
- After the simulation and before the approval/signature gate, the agent should solicit structured user feedback on strategy-shaping parameters framed by the forecasting model — deployment through the yield generation cycle (front-load carry / smooth DCA / back-load on dip), execution timing (TWAP / immediate / opportunistic), coupon-yield policy (auto-compound / distribute) — "Ask the user for feedback using the Times FM-3 model: what should be the detailed allocation throughout the yield generation cycle... what should be the timing, what should be the coupon." A model-framed HITL questionnaire sits between simulation and the Ledger/Safe signature. Confidence: 0.7
- Build-verification gate on every hand-off: the user runs production builds themselves and pastes failures back verbatim — a delivery that doesn't compile (especially TypeScript errors) is an unacceptable hand-off state. After code changes, verify with `npx tsc --noEmit` and a production `pnpm build` before reporting done (dev server stopped per the `.next`-corruption rule), and treat reported build/type errors as fix-immediately items, not advisory feedback — "resolve them ASAP and make sure the application is working." Confidence: 0.8
- The user runs their own dev server concurrently with the agent's work (theirs holds port 3000; the agent's `pnpm dev` falls through to 3001) — don't assume the agent's instance is what localho- After transport/service interruptions (e.g., "Error: Connection error... Type \"continue\" to try again"), the user sends a bare "continue" and expects the agent to resume the in-flight task exactly where it left off — same todo list, same branch, same commit series — with no re-planning, re-confirmation, or restart from scratch. Confidence: 0.85
- When the user supplies their own ideal-architecture artifacts (e.g., a Gemini-authored architecture doc like `docs/timeseries-model-architecture.md` built from example queries), treat them as the authoritative structure for the plan: rework the plan around their model (the 5-node LangGraph state machine with the Category A textual / Category B numeric time-series split), making precise the per-model data flow (how each model fetches its data, runs its analysis, and hands predictions to the agent), the shared agent state that merges both models' outputs into one comprehensive response, and the guardrails both models need to produce responsible results rather than hallucinations. Confidence: 0.8
- Model outputs — LLM structured responses AND time-series-model forecasts — must pass pure, golden-tested guardrails BEFORE entering agent state: zod schema enforcement, quantile monotonicity (q10 ≤ q50 ≤ q90), k·σ scale sanity against history, citation trace-check (uncited numeric claims = hallucination ⇒ regenerate once, then typed failure), and amount-sum invariants on the execution matrix. Breaches force a typed failure or one bounded re-plan cycle. User explicitly directed guardrails "for both models so that they generate responsible results, not hallucinations or other issues like that." Confidence: 0.75
- Multi-model support is a stated v0.1 requirement: the agent integrates the default Gemini tier AND other model families — realized as an env-driven per-role model registry (parser / synthesis roles via `LANGCHAIN_MODEL_<ROLE>`, LangChain-style resolution) so additional families plug in via env without code changes, while the deployed TimesFM-3 container owns all numeric forecasting deterministically and the LangGraph state machine owns orchestration/aggregation. Confidence: 0.7
- The EMS time-series store is TimescaleDB (Postgres extension) — the user's "Timescale/MySQL" dictation refers to TimescaleDB, per the in-repo ux-workflow observability contracts — with hypertables for pool metrics, TimesFM forecasts, and backtest runs, fed by the the-graph poller; no MySQL dependency is introduced. Confidence: 0.65
st:3000 serves; check port occupancy and point browser-verification at the port actually running the agent's build. Confidence: 0.55
fabricating connection strings or standing up paid resources. Confidence: 0.7
- Planning for substantial build/integration work follows a strict sequence the user enforced when the agent started coding directly: enter plan mode first → research → write the plan → get it explicitly accepted → only then implement. Verbatim correction: "Sorry but first you need to go into the planning format, create a plan, and then get it accepted based upon whatever research you are doing. also use /impeccable in order to define detailed planning specs in structured format." Three durable elements beyond the standing plan-mode trigger: (a) the plan must be grounded in the research just performed (a verified-findings section with checked sources, e.g., the official npm package, connection semantics, current repo state) rather than written from assumptions; (b) detailed planning specs are written in structured format using the /impeccable skill's planning shape (job & audience, outcome & proof, direction, scope & boundaries, states, constraints — rendered as a numbered planning-brief section inside the plan doc), not free-form prose; (c) the plan is an acceptance gate — the user reviews it, their additions get folded into the same plan file, and implementation starts only after acceptance. Confidence: 0.85
pSeek V4.1 Flash by the way") after noticing Claude named in commit-trailer boilerplate. Never assume the underlying model from prior attribution boilerplate; name the model the user states, in commit messages, PR bodies, docs, and any attribution line. Confidence: 0.9
- When told to hold off committing ("don't commit for now"), the user wants the entire remaining task list finished and verified first, and will decide afterwards whether the work lands on the same branch or a separate one — "once it's completely implemented for all remaining 9 items, I will check whether it has to be committed to the same branch or to a separate branch." Treat branch/commit placement as the user's call on multi-package work, and don't resume committing until they say so. Complements the standing granular-commit discipline (which governs how, not when). Re-affirmed and sharpened when the agent committed anyway — "I told you, please for now don't commit any changes... For now keep the implementation of the 17 items unstaged": the hold is total, so leave the work uncommitted AND unstaged in the working tree for the user's own branch/ticket mapping, not merely "committed locally on stage" as an earlier message suggested. Stating the same instruction twice ("I told you") means the agent must not treat a prior approval as license to commit. Re-confirmed on the 1inch Aqua/SwapVM work: the same message that asked for "detailed commits in a separate branch" was later superseded by the answer to the branch-base question — build the work in the working tree and "leave git alone entirely", no staging and no commits. A later hands-off-git instruction overrides an earlier request for commits, with no need to re-ask. Confidence: 0.9
- Wants new pipelines to be fully runnable and testable locally first: a first-class `verify:local` path that executes the whole chain (fetch → parse → validate → derive → write to the real hosted DB → upload → read back) on the dev machine against real hosted services, with cloud deployment optional rather than required for development (local-dir/`--no-cloud` fallbacks). Explicitly stated requirement, not a suggestion. Confidence: 0.75
- Requires detailed structured logging at every stage of a data pipeline so a run is auditable without re-running it: per-source start/finish with latency, HTTP status, bytes, parsed record counts, validation pass/fail naming the offending field, derived factors with their inputs, rows written per table, embedding counts, and a final summary — human-readable locally and JSON-parseable (`RISK_LOG_FORMAT=json`) so the same logs work in Cloud Logging, with the run log persisted to a gitignored `logs/` folder. Scope extension: the same disposition covers test/dry-run scripts — a verification round must emit readable, step-by-step logs (per-stage resolution, HTTP statuses, what it built, and where it deliberately stopped short) delivered as an artifact the user can read during their own testing phase ("generate the necessary logs for me so that I can read them during the testing phase"). Confidence: 0.75
- Treats code-craft standards as binding acceptance criteria backed by a dedicated enforcement phase — strict typecheck (tsc strict flags) and lint green in both languages, an explicit zero-`any`/`Any` audit where any surviving occurrence must sit at the single documented wire boundary with inline justification, public-API documentation enforced as lint errors (JSDoc `require-jsdoc` on exported symbols; Python docstring rules), typed error hierarchies instead of bare `Error`/`raise Exception`, and type-level tests — rather than leaving style to review discipline. Confidence: 0.8
- Splits storage by workload plane: Vercel Functions/Blob remain the surface for app serving and demos, but for a Cloud-Run-based ETL the snapshots live on GCP (GCS free tier, in-region, no cross-cloud egress) and the temporal history is written into the already-provisioned Tiger Cloud TimescaleDB — provisioning no new managed database (explicitly rejecting Cloud SQL/AlloyDB) and adding a budget alert instead. Reuse of existing paid infrastructure over new spend. Confidence: 0.7
- For data feeds that drive live models, wants a short explicit refresh cadence — "this data has to be fetched recently or after every 6 hours" — so downstream agents never reason on stale snapshots. Confidence: 0.7
- When offered a lighter transport that already works (plain HTTP + Discourse JSON APIs) versus one uniformly robust path, chose the robust one: "Camoufox for everything" across every source, accepting a container runtime and slower runs for a single anti-bot-proof code path — robustness/uniformity over minimalism when scraping hostile sources. Confidence: 0.7
- HARD RULE — local-only git workflow: never fetch, pull, or merge `main` into an in-progress working branch. The user explicitly instructed "don't pull the main branch and then create potential conflicts... Please don't pull the required changes from main. It will really mess up my local branch and create issues." Commit the existing work locally on the current branch and leave it exactly where it is — no push, no main interaction, no rebasing against upstream. Confidence: 0.9
- The user retains granular control of how completed work is split across branches and tickets: they expect the agent to commit locally (on stage) and stop, then they will create the branches, first merge the stage commits belonging to the current workstream (e.g., the LangChain v0.1 series) themselves, and only then direct which remaining changes map to which of their pre-created GitHub tickets ("I will help you to granularly manage the contribution of the remaining changes across the different branches based upon the different tickets that I have created"). Don't decide branch/PR placement for multi-package work — commit locally and wait for the user's per-ticket branch mapping. Confidence: 0.85
- New UI components must conform to the incumbent design system rather than invent a second visual world: reuse the existing tokens/primitives (colors, badge variants, type scale, corner rules) and treat any new idea as a single semantic addition on top of the managed base theme — "keeping the overall UX workflow similar to the base theme that is being managed." Confidence: 0.8
- When component precision matters, wants the canonical component library checked directly, along with its skill — review comment: "check the shadcn ui library along with its /skill in order to make the given codebase more precise." Look for and load the library's own skill first (via find-skills), and when none is installed use the canonical registry/docs (`npx shadcn@latest view`, ui.shadcn.com) as source of truth; hand-port + re-theme to match the repo's own conventions rather than running the CLI blind. When building a new component, the accepted routes are to install the library's component directly or to borrow how it renders — "either use their components directly by installing or take motivation" — rather than hand-rolling a one-off in isolation. Confidence: 0.8
- For UX/component work, wants an explicit design-brief deliverable authored before code, via the impeccable skill in plan mode, covering: the design theme, the color coding, and how to showcase the workflow more dynamically while staying on-theme — grounded in a frame-by-frame analysis of the supplied reference material, with the resulting Markdown file later used as the spec that drives building the component in its package. Confidence: 0.8
- Design-system/library work isn't finished at the package level: once a reusable component is built (e.g., the Sankey-style `LiquidityFlowRoadmap`), the user immediately directs it be integrated into the actual product application ("implement this into the actual application... it should render all the investments that are being currently done by a particular user portfolio"), bound to the app's real data layer so it renders the full live user state (every position with its weight, APY, VaR and owning agent) with nothing invented in the wrapper, and mounted on both the main dashboard grid and the chat's inline generative-UI widget registry — not left as a standalone demo or library-only artifact. Scope extension (1inch Aqua/SwapVM pass): the same applies to execution-layer packages — the plan for a new package must carry an explicit integration design ("a very solid architectural decision... so that it can be integrated with our own comprehensive workflow") naming which existing apps/packages it wires into (e.g. the execution service's stubbed routes, the agent's tool set, the custody signing-intent kinds) and how, plus what stays untouched so no existing behaviour regresses. Confidence: 0.8
- Wants one package per branch: each package's contribution lands on its own dedicated branch off main (the langchain branch carries the langchain package plus its app integration; a separate branch + PR carries `packages/arc`), and each commit is scoped to a single package so any commit can be lifted onto its own branch. When a forced import chain drags in dependencies (timeseries → risk → langchain → indexer), include them as clearly-labelled, separately-liftable commit series rather than leaving the branch unbuildable. Confidence: 0.8
- Main only advances through a merged branch + PR — never a direct push, even for a small urgent repair such as committing a missing package: the arc-client fix went out as `dhruvmalik007/arc-client-v0.1` + PR #8 and merged from there, rather than being committed straight to main. Confidence: 0.75
- Merges PRs into main with a merge commit (not squash) so the granular one-concern-per-commit history survives the merge. Confidence: 0.65
- Repo self-consistency over local convenience: any workspace package that committed code imports must itself be committed, because an untracked package sitting on the dev machine lets an actually-broken `main` pass the local gate. Verify the gate on a clean checkout (a throwaway git worktree of main) instead of the working tree that has untracked packages masking failures. Confidence: 0.7
- Mocks are temporary scaffolding, not the final state: once the service's credentials/provider are available (e.g., a supplied `.env`), the user directs replacing the simulated flow with the real, working integration end-to-end — after the time-boxed v0.1 demo's mocked sign-in, they ordered "integrate the actual login workflow" (real Gmail/Google SSO via Privy that provisions each user's personal smart account) rather than leaving the mock in the shipping app. Confidence: 0.72
- Prefers authentication methods that work on preview deployments without per-environment domain choreography: had Google OAuth removed in favor of email + generated one-time code login, because OAuth requires allowlisted redirect domains per environment while an email OTP has no redirect step ("for the preview support just remove the integration of the Google account. The user will just log in by putting in their personal mail and then adding the OTP that is generated"). When choosing a login provider/flow, favor the option with the fewest deployment-environment prerequisites (no OAuth redirect allowlisting) so preview and ephemeral URLs just work — and note that origin/domain allowlisting in the provider dashboard still applies independently of the login method. Confidence: 0.8
- Prefers Privy as the standard EOA provider for the EMS product: onboarding is email login → an externally-owned account, and that EOA subsequently integrates with the Ledger (device-confirmed signing) and with Safe (treasury management). Wants the whole EOA + Ledger + Safe smart-account combination validated against the provider's own documentation before it is designed in. Scope note (later pass): the account model is stated to be a Privy EOA reached through SSO, which puts on-device signing OFF the critical path — "we will be using PRYEOA from SSO, so I think on-device signing is not needed." Hardware signing stays an optional, stricter route rather than a default requirement: don't block or over-invest in device flows when the SSO EOA covers the use case, while keeping the device-ready code correct and tested. Confidence: 0.85
- Requires Ledger device signing to run on the modern Device Management Kit (DMK) stack rather than the legacy `@ledgerhq/hw-app-eth` adapter — the original requirement statement describes the account integrating "with the ledger for doing the signature of DMK clients", and the user later ordered the migration outright once its capability was confirmed ("I think the dependency on legacy hardware... should be replaced with DMK"). The criteria the user judges a hardware-signing stack by are durable: one signer kit covering every EVM chain (the chain id is part of the payload, not a per-chain device app), and full EIP-712 typed-data signing so the device decodes and displays real intent — versus the legacy path's opaque-hash blind signing. Clear Signing depends on a Ledger-issued `originToken`, which is a Ledger-side registration, so it is documented/reported rather than assumed. Confidence: 0.85
- On new requirements, expects the agent to make and state the placement call: extend the existing package only if the requirement genuinely entails it, otherwise implement the logic in the app layer (e.g. `apps/agentic-EMS`, or a new sibling folder such as `apps/auth`) — "if you think that this requirement would entail more changes needed within the packages/custody, surely put them into the plan. Else we can implement it as logic in the app/...". Confidence: 0.75
- Sets an explicit acceptance criterion and delegates the go/no-go decision, expecting the agent to research the vendor's own documentation and TypeScript SDKs, verify the condition, and then actually execute once it holds — for the Ledger signer: "Do the research about that and see whether it is posing issues or constraining our execution of the clear intent and signing across all the EVM chain support. If not, then you can not take it... Else if it supports almost all of the EVM chains, that's perfect." Report the evidence, then proceed with the change rather than stopping at a recommendation. Confidence: 0.75
- A cross-package fix is authorized when it is genuinely needed, with branch separation deferred to the user: on the LangChain e2e `.env` blocker the user said "you can indeed fix the given issue even if it's in a different package. Later on you can create a separate branch but for now just keep it on stage. I will review, aggregate all the specific changes for the specific package, and then will commit it." The one-package-per-branch discipline governs the final branch/PR layout, not which files may be edited mid-task. Confidence: 0.75
- Implement advertised surfaces rather than leaving them as documented gaps, and label any that exist only for testing/demos as such: for the missing `demo` / `safe deploy` CLI commands the user said "you can add it in the CLI switch but it will not be used in an actual use case. It was just for the tests." Confidence: 0.65
- Scopes reference-media analysis to an explicitly stated time window and expects it hard-bounded so out-of-scope content can never leak in: for the UI-workflow roadmap recording, "just analyze the first 12 seconds because from 13 seconds to 19 seconds... is not relevant to me" — clip and sampled frames cut at the boundary, with the exclusion stated in the analysis artifact itself. Confidence: 0.7
- Wants long-running on-chain/execution flows surfaced as granular live progress: every intermediate transaction hash shown to the user as each step lands (not one spinner), then a clean completion state that links into the agent dashboard, where the run is stored as a card carrying its allocations and transaction links. Confidence: 0.75
- Demands read=sign parity for on-chain execution: the confirmation popup must present exactly what will be signed — "whatever the user sees and whatever the user wants to execute should be the same" — with the natural-language intent first converted into a structured, secured payload (JSON in the format the ledger/signing preview expects) before the user clicks Sign. Confidence: 0.75
- Wants fees and slippage decomposed and explained layer by layer — each sub-fee per leg, each fee's rate/source, and an explicit separation of what is actually charged versus the slippage bound accepted — rendered through the Sankey-style flow component rather than collapsed into a single total. Confidence: 0.7
- For lengthy execution flows, wants the component minimizable / put in the background of the chat so the run continues while the user keeps working, restorable on demand. Confidence: 0.7
- Deliberately defers the execution/settlement layer's definition ("we will decide later how this execution layer should be defined") and wants the UI component built now against a swappable typed adapter with simulated quotes/fills behind it, so the real middle routing layer — cross-chain bridges plus DEX aggregators, choosing the best rate at minimum slippage — drops in later without touching the UI. Confidence: 0.7
- For Web3 product UI, wants the design authored to the standard of a professional UX designer who also has Web3 domain expertise (explicitly via the impeccable skill), so the result reads as web3-literate rather than generic UI work. Confidence: 0.7
- Wants heavy execution/coordination logic built as its own standalone service — a new `apps/` entry point, containerized and hosted independently — explicitly separate from the frontend Next.js app (contrasted: "unlike the app/indexer, which has been defined in Next.js") and from the reusable `packages/`. When the user defines a new responsibility (e.g., a per-user execution layer), the default is a new standalone microservice/roadmap, not more code folded into the existing web app. Confidence: 0.65
- For multi-user persistence, "stored for each person separately" means per-user scoping via a tenant key — NOT a database or table set per user; the user corrected the agent for over-reading the phrase. What matters to them is the ownership/lineage chain (user → session → strategy → run → intent → step) and that the data model be driven by the dashboard's live read needs: an explicit parent→child key chain plus deliberate denormalised tenant keys and indexes so each dashboard panel is one scoped query, and every read/subscription is derived from the authenticated user rather than client-supplied ids. Confidence: 0.65
- Wants credential/secret files kept out of version control as a standing safety rule, with defense-in-depth ignore rules at the repo root rather than relying on a single package-level `.gitignore`: directed that the plaintext TimescaleDB creds file be covered by a root `.gitignore` pattern (e.g. `*-creds.md`) "so that they're not committed by accident into the main branch of the repository." Prefer broad, root-level patterns that survive package restructures/renames or a lost nested ignore file, and verify the rule both applies and hides nothing already tracked. Confidence: 0.75
- Treats performance/iteration-speed optimization as a prerequisite gate that outranks pending feature work: when compile/render times balloon, wants the agent to PAUSE the queued feature items and first shed load ("Before progressing any further and wasting the tokens for reasoning... I need you to first and foremost, before working on these six items, do the transition to server-side rendering and do many more optimizations"). The explicit motivation is protecting future velocity — optimize the loop now (compilation time, render/CDN path) so later feature additions "will not take that much time." Confidence: 0.8
- Wants Next.js apps architected server-side-rendered / RSC-first, with client components confined to leaves and data resolved on the server rather than in client `useEffect` fetches: explicitly directed "do the transition to server-side rendering" and "optimize and make this application, for now, server-side rendering so that it will be much easier," via compile-time config (Turbopack dev, optimizePackageImports), CDN/edge caching, SSG/ISR for data pages, `next/dynamic` for heavy client widgets, and server data readers replacing the client fetch waterfall. Confidence: 0.8
- Deprioritizes comprehensive database security at this stage: role-based/row-level security is explicitly not the priority and "can be delayed later" — the main issue is getting the core phases/workflow complete and testable. Per-user `user_id` scoping in queries is the accepted working control, with RLS treated as optional defence-in-depth to be added in a later pass rather than half-built now. Confidence: 0.65
- Rejects hard-coded per-item pricing in favour of a composed cost model that mirrors how a reference protocol prices its routes: LI.FI prices a swap *route* by composing the cost of its parts, and the user wants strategy pricing built the same way — a pure, per-strategy-kind function (gas + protocol/vault provider cut + pool fees + price impact) that composes components rather than a constant defined per execution. Protocol take-rates should be read from a live source of truth (e.g., fee/revenue ratios from DefiLlama) instead of assumed percentages, and a fee already `included` in a quote must not be counted again. Confidence: 0.75
- Test scope for a new integration package is deliberately bounded: "please don't do extensive tests. Just do enough tests that you are able to fetch the relevant pricing or route data and other details" — enough to prove the real integration returns live quotes/routes/status (per-adapter fixture mapping tests plus one opt-in live check, no funded-wallet/money-movement tests), rather than an exhaustive suite. Right-size coverage to prove the capability instead of maximizing test count. Re-affirmed on further integration work under deadline: "Don't overengineer it. Don't create too many tests or some things that can take crucial time now, as I have around 12 hours" — when the user states a hard time budget, deliberately cap the build and the test suite to what proves the integration works rather than delivering the full comprehensive suite. Confidence: 0.8
- Work ordering for new packages: build them out comprehensively first (full ports, both provider adapters, tests living inside the package), then evaluate whether they actually work against the real APIs, then integrate into the consuming app (e.g., `apps/execution`) and only then take on the remaining tasks — explicitly no half-built adapters and no premature wiring into the service. The user lays out this ladder themselves and expects it honored: finish building the wrappers completely → run a testnet verification round (mock transactions against real smart contracts / liquidity pools) → only then "progress further on the remaining" backlog items. Live-chain verification is deliberately deferred until the implementation is complete, not interleaved with it. Confidence: 0.75
- When a route is blocked on a dependency that does not exist yet, an explicit, honest "not implemented" fallback is acceptable to keep for now ("If you have implemented a clear fallback, it is fine for now") — prefer a clearly-labelled refusal (e.g., 503 UNAVAILABLE) over a faked result, and fill it in once the real adapter lands. Confidence: 0.65
- Protocol-facing packages must expose protocol-AGNOSTIC interfaces, designed to OO/SOLID ("the type description using ZOD and other descriptions that these packages should implement agnostically from these various APIs using OOPS and solid principles"): normalized zod-typed types at the boundary, and a public surface that never names the underlying vendor (1inch, LI.FI, LayerZero) — each provider is an adapter behind a port, so adding or swapping one is a new file rather than a change to the consumer. Expects the class methods and their documentation to be built against that port surface. Confidence: 0.7
- Assigns cross-cutting execution concerns to the CONSUMING APP layer, not the protocol package: edge cases such as a bridge message that is mined but never delivered, or off-chain-order SDK/contract version skew, are to be resolved in the import point (e.g., `apps/execution`) — packages stay report-only and therefore swappable, while the service owns deadline/timeout states, pre-flight validation and policy. Confidence: 0.7
- In agent/task-trace UIs, curate the expanded view instead of dumping everything: show only the legible, necessary details — why the step ran, what data it ingested, and what it produced — with raw payloads parked behind a nested disclosure that is closed by default. Explicitly: "we should not be putting everything. We will be just rendering the legible or just necessary details in the dropdown," framing the same three-part contract (reasoning / ingested data / result) as the one the productionised rendering will carry. Expects the agent to emulate how established libraries render reasoning traces rather than inventing the information architecture. Confidence: 0.75
- Prefers packages to stay decoupled — explicitly minimizes cross-package dependencies ("i dont want too much of cross package dependency") even when it accepts limited duplication of a small protocol constant as the price, e.g. choosing to keep `packages/arc` separate from `packages/bridges` and have the bridge package depend on Circle's own provider SDK directly rather than importing the sibling package's `CctpV2`. When faced with a reuse-vs-couple choice between workspace packages, decoupling is the default. Confidence: 0.75
- Each new package should ship a `.env.example` documenting every credential and config it needs, with a note on why each is required (e.g. which keys the live path needs versus which read on-chain and need only an RPC) — and the user expects to be told explicitly which API keys they must obtain once implementation is done ("if you need API keys for any of the above protocols mentioned, do indeed let me know once you finish implementation (by adding in .env.example in their respective packages)"). Confidence: 0.7
- For stalled/at-risk on-chain flows, prefers a simple bounded recovery path over elaborate detection designs: for a bridge message that is mined but never delivered, the stated solution is to keep sufficient gas plus a sufficient approximate number of retries, and if the transaction still does not go through, to faithfully cancel and retrieve the funds/data — recovery over mere alerting. Tends to reframe agent-invented prevention/detection tables into the shortest robust mechanism that actually returns capital. Confidence: 0.65
- Extends the vendor-tooling-first research discipline to query servers: when a vendor ships an MCP server or skills package, directs installing that vendor's own surface for "more specific queries resolution" during planning (e.g. Circle's MCP at developers.circle.com/ai/mcp) rather than designing from assumptions. Confidence: 0.65
rrent build there, verify it live (routes, SSR, inlined public config, no leaked secrets), and restore any pre-existing project links the CLI rewrote. Staging is the user's own UI/UX acceptance surface for the current build, not a stale snapshot. Confidence: 0.7
- Treats an approved implementation plan as a binding scope contract: once the plan is accepted, every deliverable it enumerates must actually be implemented as specified — on the bridges/execution-layer work, "follow strictly the plan that I have provided you and make sure that the three possible routes (Layer 0, LiFi, and CCTP) are implemented as defined in the plan." Silently implementing a subset, or substituting a different approach, is not acceptable; a genuinely better alternative is welcome only when explicitly flagged. The user also provisions the required credentials themselves per package (they set up the real `.env` in `packages/bridges` before asking for the adapters), so missing keys are never an excuse to leave an item unimplemented. Confidence: 0.7
- Wants each provider's fee quotation built as its own math class/function rather than one unified calculator: for the bridges asked for "separate fee quotation math classes or function math scripts" — one for LayerZero, one for Circle CCTP — because the two quote mechanisms share no inputs (an on-chain OApp/endpoint read with option bytes vs. a signed quote API with a speed parameter). Provider-specific logic stays isolated per provider; mirrors the separate reader ports (LayerZeroFeeReader / CctpQuoteReader) rather than branching inside one function. Confidence: 0.7
- Expects the concrete integration surface researched and verified up front from authoritative sources before implementing a protocol: contract addresses from DefiLlama or the protocol's own GitHub repo/docs, the specific APIs/endpoints that return execution price, and the prominent public RPCs for each chain. Research-and-verify first (via /web and /skills), then build. Confidence: 0.7
- Scopes a broad protocol landscape down to a bounded, representative set instead of integrating everything — explicitly imposed the constraint of taking only the top two protocols per category across lending, staking, prediction markets, perpetuals, and swaps, to keep the integration surface demonstrable and verifiable. Confidence: 0.65
- Wants integration verification to exercise the full COMBINATORIAL matrix, not a single happy path: for the bridges work, directed testing "across the various permutations and combinations" of chains, tokens (USDC or EURC) and providers — i.e. every meaningful pair/path across all the implemented methods, with each test kept small. Coverage breadth across the implemented matrix is the point, not one passing route. Confidence: 0.7
- Expects the agent to use the already-funded test wallet rather than asking for faucet funds: the user keeps a `PRIVATE_KEY` in the package's `.env` holding real testnet USDC/EURC (arc testnet, Base Sepolia, Ethereum Sepolia) and states it is "sufficient in order to test by sending small amounts" — so a test round should proceed to actual small testnet sends off the supplied wallet instead of requesting funds or stopping at quote-only. Missing funds are not a blocker to be escalated; the user has already provisioned them. Scope extension (mainnet pass): the same wallet-ownership model covers real mainnet funds — the user topped up the Polygon wallet with a deliberately small real balance ("100 POL tokens") and told the agent to reuse the existing `.env` PRIVATE_KEY, so mainnet verification rounds proceed on the user's own funds with no faucet request and no escalation. Confidence: 0.7
- Verification expectation for execution/integration flows: fetch the quotes LIVE from the real production/mainnet APIs (never mocked), build the corresponding swap order/transaction from those quotes, and print each step's result as the flow runs — but as a SIMULATION only, with no trade broadcast ("Not executing the trade, its simulation of the trade"). Live read → constructed transaction → narrated per-step output is the deliverable of a verification round; actual execution stays explicitly out of scope until the user says otherwise. Confidence: 0.7
- When a provider exposes a generous no-key free tier, prefers sending requests keyless by default and attaching the API key only as a fallback for throttling/rate-limit retries — not as the default auth path. Directed for LI.FI (100 RPM free tier): "not use the API key for now... Just use the LiFi API key as a fallback in case we are using or throttling too many queries," implemented as keyless requests with the key gated behind an explicit opt-in (a `--with-key` flag). The underlying disposition: don't block a working integration on a misbehaving credential — get the live quotations flowing without it, keep the key as the escalation path, and make sure every provider is actually returning good quotes rather than stopping at the auth error. Confidence: 0.7
- When directing a new chain/protocol integration, points the agent at an existing open-source implementation (a named GitHub repo, e.g. `hoanghieu512/arc-cctp-bridge`) and asks "which components we can implement into this package" to enable a specific capability (e.g. simulating a fee quotation / potential transaction from a source chain to the target chain). Expects a component-level adoption review against the current package — what to port, what to adapt, what to leave — rather than wholesale adoption, a from-scratch rewrite, or a mere summary of the reference repo. The same directive pairs repo-mining with an explicit charge to read the target chain's up-to-date official documentation via `/web` first ("for really checking out what the up-to-date implementation is, please extensively check the ... documentation using /web") so the port is grounded in the current spec, not the reference repo's possibly stale snapshot. Confidence: 0.75
- Uses Alchemy as the default hosted EVM RPC provider for web3 work: keeps an Alchemy key in the package `.env` and supplies the full per-chain mainnet endpoints (`https://<chain>-mainnet.g.alchemy.com/v2/<key>` for Ethereum, Polygon, Base, Arbitrum) as the canonical values, expecting the agent to format them into whatever env-variable scheme the code requires (including composing a multi-chain RPC map such as `LAYERZERO_RPCS` from the same key). Prefers these keyed hosted endpoints over keyless public RPCs, and delegates URL/format correctness to the agent ("Kindly add in the correct format the URL scheme as you require"). Confidence: 0.7
- When implemented-but-unfinished work remains blocked on missing pieces, wants the gaps captured as a written ROLE MAP document rather than left as prose caveats: every missing piece classified by owner — what the agent implements itself, what the user must provide/copy-paste, and what the user must verify — so the user knows exactly what is expected of them. Directed for the CCTP/LayerZero ABI-and-address work: "use the necessary /find skills in order to create a detailed role map on what missing pieces there are and whether you need me to implement them, copy-paste them, or verify them, you can let me know." The document (e.g. `packages/bridges/docs/missing-pieces.md`) should sit alongside the package, state which items need no outside help, and distinguish "confirmed present/researched" from "confirmed by name only, signature not yet read". Confidence: 0.75
- Prioritizes simplicity over minimal trust as an explicit trade-off criterion: stated verbatim "for me the priority right now is more simplicity rather than minimal trust," and applied it by choosing a plain EOA paying native gas over an ERC-4337 paymaster/smart-account setup (rejecting an account-abstraction stack to save a few cents of ETH), and by DROPPING a redundant Stargate path rather than adding a fourth bridge route with its own OFT addresses and quote flow for coverage already provided. Prefer the smallest number of moving parts that accomplishes the goal; a trust-minimizing option that adds integration surface/complexity loses to the simpler one, even when the user raises the trust-maximizing alternative themselves. Note this scopes to redundant/duplicative complexity: the multi-provider coverage mandate (LI.FI + LayerZero + 1inch across categories) stands. Confidence: 0.8
- Floats tentative half-remembered assumptions about external facts (e.g., "I think you can sponsor the gas fees for X number of transactions. I don't know whether it's true or not. Can you check that?") and expects the agent to verify them against authoritative sources rather than accept or dismiss them — then state plainly whether the assumption holds, partially holds, or is wrong (the sponsorship/allowance belief turned out to be USDC-as-gas via the paymaster, not a free credit). Related to the research-first discipline, but specifically about fact-checking the user's own hypotheses before building on them. Confidence: 0.7
- Wants core integration logic kept WALLET-PROVIDER-AGNOSTIC, with the concrete signer bound only at the demo/script/integration layer: directed that the CCTP/LayerZero transaction-building work be wired for the demo with the private key in `.env` now, but architected so the same code is later "agnostic for accepting any wallet provider." The realized shape is pure encoders/builders that take parameters and return an unsigned `{ chainId, to, value, data }` (no signer, no provider, no key), with only the caller/script deriving an address from `PRIVATE_KEY` — so swapping in Privy or any other provider means changing the caller, not the core. Design new execution primitives to this separation. Re-affirmed on the Polymarket Bridge deposit flow: the private-key run is explicitly framed as the TEST CASE, and the real requirement is that anyone can then run the same flow agnostically — "work on how you can allow anyone to do that agnostically once it's tested for the particular private key use case." Treat `.env`-private-key wiring as a temporary harness around a provider-agnostic core, never as the delivered design. Confidence: 0.85
- When the user fixes a goal and escalates ("Now you need to do this task at all costs. You need to make sure that both ... are able to create transactions"), they expect it driven to a working, verified implementation rather than another round of honest "not done / nothing committed" gap reporting. Repeated status updates that stop short of the goal are insufficient once the objective is fixed; change approach as needed (e.g., encode against the deployed contract instead of fighting an inference-only API) to actually land the feature. Confidence: 0.75
- For major new systems/microservices, wants a named open-source project studied as the explicit architectural motivation and expects it mapped concretely onto the repo's own stack: directed the inference microservice be designed "taking the motivation and architecture from the famous project E2B runtime" (github.com/e2b-dev/runtime) with a deep-dive via /web and /skills in planning mode. A component-by-component mapping onto the existing packages is the deliverable, not a generic summary of the reference. Confidence: 0.65
- Wants major build work to produce a very detailed product-design roadmap as a first-class, committed artifact — architecture, locked decisions, and an enumerated, decomposition-level task list — that is published from the repo and linked from the GitHub project page/README, rather than delivered as chat prose ("write a very detailed product design roadmap with various tasks that I need to implement, which will be added as a link on the GitHub project page"). Confidence: 0.7
- Requires plan-item → evidence traceability in status reporting: each checklist item claimed done must be backed by its own named proof (the specific test file and the behaviours it asserts for THAT item), not by aggregate signals like a live migration plus a suite-wide pass count. The user rejected a completion report that showed the migration and the bridge tests but "never produced evidence for the repositories themselves," and the accepted fix was to write the missing module tests so every T-item maps to a named artifact. Corollary: a shipped module with no tests is treated as an unfinished item, not a done-with-debt one. Confidence: 0.7
- Prefers integration/venue testing against a provider's own simulated/paper-trading/sandbox environment over real funds, and expects the agent to first establish whether such an environment actually exists — probing the candidate hosts rather than assuming one is there — before deciding how to test. "Ideally do the actual tests on the simulated platform", with "or let me know if it is not possible" as the fully acceptable answer when no sandbox exists (CORRECTION — Polymarket V2 order pass: a simulated venue DOES exist — the official V2 SDK `@polymarket/client` ships `production`/`preproduction` environment configs plus `forkEnvironmentConfig`; only the old `clob-v2.polymarket.com` test host fails to resolve, and generalising from that to "no sandbox exists anywhere" was too broad. Prefer the SDK's preproduction/fork environment before any live-funds order). Real money is only the fallback, and deliberately small ("I will transfer some small amount to the actual platform"). Confidence: 0.75
- When a NEW on-chain step is needed, wants it built on the already-proven adapter/code path rather than a fresh integration: directed the funding swap be done "following the experience that you have got by implementing the LiFi SDK" and to "use that bridge transaction" for the small POL→USDC conversion — i.e., route new fund/execution needs through the existing provider adapter (its quote → build flow), reusing working plumbing instead of hand-rolling a new DEX/swap path. Confidence: 0.7
- Delegates the choice of test subject for live end-to-end runs to the agent, as long as the flow is real: when directing the minimal live Polymarket order on pUSD-collateralized V2, specified "you can take any market ID on the Poly Market and then try to execute a particular market order" — no specific market matters, only that a genuine minimal order is executed against the live venue. Don't stall on which entity/instance to use; pick a representative one and proceed. Confidence: 0.6
he custom builder ("implement the fix for generating the one order with the official V2 SDK") instead of reconstructing the wire format from documentation tables. The SDK's fast, named failure is the deliverable: it settled the exact field encodings (numeric `side`, market-derived `feeRateBps`) by observation. Confidence: 0.8
- When a live order/execution test must pick a venue, wants a LIQUID market and its freshest live feed referenced first: "get the latest feed of the particular market ID, which is Liquid" — fetch current market state (tick size, tradability, fee rate) and trade the liquid/small-size market rather than a stale or illiquid one. Confidence: 0.7
- The repo's EVM stack is viem (createPublicClient/createWalletClient, privateKeyToAccount, parseAbi, viem/chains), not ethers — new on-chain scripts should be written against viem, and an ethers dependency pulled in by a third-party SDK should be swapped for the viem equivalent (the CLOB SDK inspects the signer for `signer.account`, which an ethers `Wallet` lacks). Confidence: 0.7
- Polymarket's official V2 SDK is a different first-party package family, not a newer version of the legacy client: `@polymarket/client` (alongside `@polymarket/bindings` and `@polymarket/types`) is V2, while `@polymarket/clob-client` — 5.8.1 being the latest published — is the V1 legacy client, which is why `pnpm add @polymarket/clob-client@latest` changed nothing while the server kept answering "invalid order version, please use the latest clob-client". When a venue says "use the latest client", enumerate the provider's registry packages to find the real first-party family before assuming a version bump. `@polymarket/client` exposes exactly the surfaces this work had been hand-rolling — `ProtocolVersion`, `tradingActions` (with `PlaceLimitOrderError`/`PlaceMarketOrderError`), `buildHmacSignature` (replacing the hand-rolled L2 HMAC), `SignerType`/`WalletType`/`SignatureType` (the wallet-agnostic seam Privy/Safe/Ledger DMK map onto), `createSecureClient`/`createPublicClient`, `encodeSafeMultisendCall`, and `production`/`preproduction` environment configs plus `forkEnvironmentConfig`. Directing "standardize the SDK V2" means wiring to that family (reading its configuration types first, the discipline that finally worked on the bridges) and deleting the hand-rolled order builder rather than continuing to patch it. Confidence: 0.8
- The order-placing V2 CLOB client is its own published package — `@polymarket/clob-client-v2`, per the Polymarket/clob-client-v2 GitHub README — distinct from both the V1 legacy client and the unified `@polymarket/client`. It takes an OPTIONS-OBJECT constructor (`{ host, chain, signer, creds }`) rather than the V1 client's positional arguments (passing a viem wallet client into the V1 slot produces "wallet client is missing account address"), and it exports `Chain.AMOY` — a further correction to the earlier "no sandbox exists anywhere" generalization. V2 also refuses an EOA maker outright ("maker address not allowed, please use the deposit wallet flow"): trading must go through the deterministic Deposit Wallet/proxy flow, whose approvals are gasless and therefore require a Relayer API Key (or Builder API Key) supplied as `apiKey: relayerApiKey({ key, address })` in the client options. The deposit wallet address is read from `client.account.wallet`, and the EOA has to be funded into that wallet rather than traded from directly. Confidence: 0.75
- When the remaining blocker is a single credential the user owns, the user resets/reissues it themselves and then expects immediate real execution — "I'm going to reset the key of the relayer along with the address. Now if you will execute, you might be able to deploy this and run this application. Currently the relayer is deployed on production." Treat the replacement credential as the unblock signal: re-run the full end-to-end flow against their production deployment rather than re-planning, re-confirming, or downgrading to a sandbox. Confidence: 0.7
- Treats abandoned hand-rolled integration work as salvageable, and expects the distinction stated rather than the effort dismissed: from the Polymarket V2 pass the earlier custom path was still credited with what it proved — the on-chain funding path, production authentication, the verified signer recovery, and the fee-rate diagnosis that explained the original rejection — with the mistake named as where the order-building lived, not the diagnosis itself. When swapping a custom implementation for the official SDK, report what it established as evidence and what is now superseded. Confidence: 0.65
- Wants new backend capabilities proven end-to-end from the backend/CLI side FIRST, with UI/app integration treated as a later, separately-paced phase: for the inference pipeline directed "now start working on testing the end-to-end workflow via CLI or backend format" and "start the testing from the backend side and then accordingly we will progress further on integrating it with apps/agentic-EMS." Order of work is backend service runs and streams correctly via CLI → only then wire the front-end app. Confidence: 0.8
- For the deployment/inference tier, prioritizes runtime performance and low overhead over feature richness: asked for the pipeline on a "Gen1 inference solution, which is kind of like bare metal and has good performance," even though that choice excludes in-instance sandboxes (which require gen2) and forces untrusted execution behind a separate service. Prefers the fastest-starting, lowest-overhead execution environment, and expects the agent to surface the resulting architectural trade-off (and resolve it) rather than silently overriding the stated request. Confidence: 0.7
- Wants observability wired per operation, not just per run: asked how to "provide integration of the observable metrics of the traces for each operation to the LangSmith dashboard" — every step of the pipeline (agent steps, tool calls, sandbox executions, model calls, custody/signing actions) should be its own traced operation carrying its metrics so they are visible/aggregatable on the dashboard, rather than one opaque top-level trace. Confidence: 0.75
- Wants shared cross-cutting contracts defined ONCE in the domain-owning package and consumed from there — never duplicated in the app/service layer: corrected that the signing-intent standard belongs in `packages/custody` ("let's say use this package /custody to define the signing intent and integrate it"), with the inference app importing/re-exporting custody's definition instead of maintaining its own copy. Confidence: 0.7
- Cuts off over-reasoning and demands execution: when the agent is deep in analysis/explanation, the user intervenes directly — "You should not be doing reasoning" — and redirects it to finish the in-flight task (e.g., the live order-execution test) first and only then move to the remaining items. Long deliberation and repeated status write-ups are not the deliverable; run the pending test, report its result, and work down the task list. Confidence: 0.75
- Keeps integration credentials in each package's own `.env` and points the agent at that file rather than pasting every value: when asked for the Privy app id/secret and the LangSmith key, the answer was "you can check in the packages/ux-workflow/.env file" and "you can also check in packages/langchain" — i.e. the per-package `.env` is the canonical credential store, and the agent is expected to locate and read what it needs from there (echoing only non-secret values). Re-confirmed on the live signing path: the user placed the Privy authorization key and its key id in `packages/custody/.env` (the package that consumes them) and simply directed the agent there — "I have defined the priv key ID as well as the personal private key in the following .env file and custody package ...". Credentials belong in the consuming package's own `.env`, not centralized or pasted. Confidence: 0.75
- The user's GCP work runs in project `ultimate3dreconstructionstack`, region `us-central1` — confirmed via `gcloud config get-value project` after the user offered an approximate name ("I think is Ultimate 3D Reconstruction Stack"), which the agent is expected to resolve/confirm through the CLI rather than take literally. Confidence: 0.7
- Prefers local mainnet-FORK testing as the verification environment for on-chain/Solidity work: for the 1inch Aqua/SwapVM contracts the testing direction was explicitly "use local fork... which will use Fork on the mainnet", built as its own app entry point (a fork-execution harness on Foundry/Anvil) rather than a mock-only or testnet-only suite. The fork run should execute real transfers against the real deployed contracts and record the evidence (pinned fork block, deployment addresses, tx hashes, pre/post balance deltas) as committed artifacts, not stop at green unit tests. Forking is the free route to exercising real venues without moving funds, and it complements — never replaces — the small-value live-test rule for irreversible operations. Scope note (Uniswap v4 wiring pass): the deciding factor is liquidity DEPTH — the user endorsed the agent's own recommendation to verify v4 on a mainnet fork "with real depths in the liquidity pools," because v4 is deployed on testnets but testnet pools lack the stablecoin depth a flight's destination needs, so a testnet run would yield swaps with no meaningful counterparty and numbers that mean nothing. Mainnet-vs-testnet is a data-realism call here, not only a cost one. Confidence: 0.7
- Prefers the standard, universally-deployed primitive as the integration target over a niche or partial-coverage one: overrode a plan that made the ETH+Base-only Morpho Midnight the rebalancing venue, choosing standard ERC-4626 Morpho Vaults instead so the leg works uniformly across the whole phase-1 matrix (Ethereum, Arbitrum, Optimism, Polygon) with no per-chain degradation, and maps onto the execution domain's existing hop kinds (`deposit`/`withdraw`) with no new vocabulary — the niche venue is kept only for the one job it alone does (fixed-rate instruments). Confidence: 0.6
- Wants threshold-triggered risk-off automation as a first-class capability: if a position's realised yield falls under a floor, or its volatility breaches a cap, the system should automatically execute the defined on-chain swap and convert the position into a stablecoin (stated as the product vision for the Aqua track). The trigger logic should be explicit and deterministic — thresholds, reason codes, and the exact steps it compiles to — not ad-hoc branching. Confidence: 0.6
- De-mocks the entire product surface once the real backend exists, at trace granularity: when wiring the deployed agent pipeline into the actual application, wants every previously simulated or mocked interaction, sub-step call, and agentic call replaced by output produced from real agent traces, real tool calls, and real tool-call results — stated as "make sure that every interaction, every substep call, and every agentic call that was previously simulated or mocked is now actually generated by agent traces, agent tool calls, and agent tool call results." The stated purpose of the integration is to test the release "on real data rather than mock data," so a rendered step that does not trace back to an actual agent callback (e.g. a hardcoded trace-builder module) is a defect to be deleted, not acceptable placeholder chrome. Confidence: 0.8
- Prefers scoping new work to fit an existing shared contract rather than widening the shared type, and will not duplicate an integration that already exists elsewhere: restricted the 1inch Aqua/SwapVM chain matrix to Optimism + Polygon (the intersection with `execution-domain`'s `CHAIN_KEYS`) instead of extending the domain's `ChainKey` union, preserved the already-verified Ethereum/Arbitrum addresses in a reference doc rather than carrying entries no leg can name, and kept the new integration at two chains because `packages/bridges` already covers large-order 1inch routing ("we can keep it now for two chains only"). When the agent reports that a package's natural scope conflicts with a shared domain constraint, narrow the package; when an integration already exists in a sibling package, scope the new one to what is genuinely additive and say so. Confidence: 0.7
- Distinguishes material findings from immaterial ones and delegates the immaterial to the agent: on two flagged technical issues replied "I can't tell… if they are not going to affect that much, you take your best call on what the best solution is to solve these two issues," and treats already-resolved items as needing no further reporting. Minor/incidental issues are the agent's call (pick the best solution and proceed); only decisions with real consequence come back to the user. Confidence: 0.65
- Do the sourcing work before escalating for inputs: when the agent's report named "the execution addresses you mentioned didn't come through" as the blocker, the reply was "can you maybe share again the execution addresses that you want me to share with you? If ideally possible can you please determine those addresses using /web, for example, and then start working on Stream D and other tasks" — i.e. first check what the repo already holds (existing `test-scenarios.json` / address books supplied most of the values), then research the rest from primary sources via /web and verify it against reality (all 11 venue addresses were confirmed with live on-chain bytecode via `eth_getCode`, and the two Sepolia USDC tokens were proven distinct by symbol/decimals calls), and ask the user only for the short, named list that genuinely cannot be determined (the LayerZero OApp address; the unverified Lido/EigenLayer Sepolia deployments). Never present "I need you to provide X" as a blocker before that search has actually been done. Confidence: 0.7
- Before implementing, wants the agent aligned with the project's own established conventions by consulting Command Code's knowledge base and loading the coding-conventions skill: the directive for the 1inch Aqua flight work opened with "First you need to take the flight scenario and then do the wiring of the remaining adapters and other implementations" and closed with "/commandcode-knowledge-base /skills coding connventions". Treat the learned preferences/prior art plus the named conventions skill as the standard new code is written against — not only the task description — and check for the repo's own conventions files first. Confidence: 0.7
- Pragmatic feature-scope triage: will not sink time into a complex or possibly-infeasible integration — "I don't want you to waste time implementing a staking or liquid staking project like on Eigenlayer if it is not possible or it's too complex." Wants to be told plainly and early when something is too tough ("If the staking is going to be tough, let me know"), and explicitly accepts a cheaper substitute — a simulation-based / fork-execution worked example of staking on Lido — over a partial or half-built real implementation. Prefer an honest feasibility call plus a bounded demo path to burning effort on a feature that may not be achievable. Confidence: 0.7
- For a serverless frontend (Vercel) calling a private, `--no-allow-unauthenticated` Cloud Run service, prefers minting a Google identity token from a service-account key (with `aud` = the service URL), adding `google-auth-library` to the app, as the primary production path — while also keeping the local `gcloud auth print-identity-token` route for dev/staging. Treats the deployed-environment auth path as a real design decision to be reasoned about and answered, not an implementation detail deferred. Confidence: 0.65
- Venue/capability enablement must be a visible, consented decision presented at two moments — the simulation stage ("what would using it buy us") and then the approval stage ("shall we") — with the same measured efficiency figure shown in both so the two stages cannot disagree about how much was at stake; a feature/venue that turns itself on is explicitly unacceptable ("I think it will be important that you mention it during the execution simulation stage and then subsequently the approval stage, where you can give either the user the opportunity to enable 1inch or maybe it will be an agent that will be prompted to execute 1inch if there are significant efficiencies"). Consent attribution is threshold-based: when a measured efficiency delta is significant enough (per the strategy's mandate), an agent may be allowed to enable it on its own; otherwise the consent belongs to the operator. Confidence: 0.7
- Wants a compact in-flow UX workflow component (inside the chat/execution message itself, not a separate page) that explicitly showcases that a venue/contract has been enabled and that execution is currently being done — the enabled/live state stated outright (explicit badge/rail) rather than implied by omission, built from the incumbent design system's existing primitives and tokens. Confidence: 0.7
- Names future work explicitly as out of scope for now and expects it acknowledged/recorded rather than implemented on the spot: on multi-chain deployment of a venue, "it is not in the current scope. I don't know where it's going to be implemented but you have to also deploy 1inch across the various chains later on. For now focus on working on the remaining items in the series." Capture deferred items in the plan/docs (and correct any inaccuracy in how they were described), then drive the outstanding items on the current list to completion. Confidence: 0.65
- Wants chain/RPC access resolved with explicit layered precedence and a single shared fallback: a chain's own per-chain `*_RPC_URL` (the scheme established in `packages/bridges/.env.example`, populated with the Alchemy endpoints the user supplies) wins, and one shared `ALCHEMY_API_KEY` expanding to per-network URLs is the fallback so a single key covers the whole multi-chain matrix in a productionized deployment — with test runs free to exercise both sources. Critically, the SAME resolution rule must be shared by every app that reaches a chain (the fork-execution harness and the live execution service): a fork that connects by a different rule than the live path would be validating a configuration nobody actually runs. Confidence: 0.7
- When integrating one version/tier of a protocol, expects the agent to also verify the workflow against the newer/parallel version rather than treating the asked-for one as complete coverage — directed that integrating Uniswap V3 pools is not sufficient and the current workflow "actually integrated with V4 pools also" must be verified. Wants any hard constraint on doing that in a real-time testnet use case surfaced explicitly (and answered with evidence), and if a live testnet implementation is genuinely not possible, a simulation demonstrating the investment in the new version is an acceptable substitute ("If it's not possible we can show simulation for investment in our V4 pools"). Confidence: 0.65
- Requires venue admission to be provably UNGATED — no KYC, no allowlist, no wallet-address conditions, no other mechanism that can refuse participation: "you need to invest in hooks where there is no blocking in terms of KYC, wallet addresses, or any other certain kind of conditions." Permissionless access is a hard selection criterion for on-chain venues, not a nice-to-have, and it must be a property that can be *proved* from the venue's own declaration (e.g. the hook's permission bits / a zero hook that makes no external call) rather than inferred from a soft "access: none"-style field or assumed from the chain. Report the verdict honestly when it cannot be proven (unproven ⇒ simulate) instead of claiming unrestricted access. Confidence: 0.8
- Wants the actual live pool/venue set established from real, current data rather than published lists or recalled addresses — for the v4 work, directed using /web "to find actual liquidity pools, like real-time use cases that will be helpful for fixed-income investors." Discovery must be grounded in what currently exists on-chain with real depth (and, where a published list is stale, probing the chain itself), and the pools chosen should serve the declared use case (deep stablecoin pairs for fixed-income) — not just any pool that answers a structural query. Confidence: 0.7
- When the agent has stated a recommendation, treats a follow-up instruction to act on it as accepted scope: "progress further on that recommendation that you have provided with my own inputs" — execute the recommended path with the newly added constraints folded in, rather than re-planning from scratch, re-presenting options, or stalling for another confirmation cycle. Confidence: 0.6
- For sponsor/protocol integrations, package placement is judged by discoverability to that protocol's own team: the user's stated motivation is reachability and credit with the sponsor ("this has to be shared with the hackathon... Uniswap core team, as it will be helpful for me to get a better chance and better reachability for them"). Protocol-specific work belongs in its own protocol-named package rather than nested inside a competing provider's package or buried in a generic/placeholder execution layer — the Uniswap v4 adapter was moved out of `packages/oneInch` into `@ethonline2026/uniswap`, on the precedent of `packages/arc`/`@ethonline2026/arc-client`, so the protocol's team finds the integration under their own name (a Uniswap engineer should not have to open a 1inch package). Corollaries: a provider's SDK must not be the dependency through which another protocol's adapter is reached (duplicate a one-line shared type rather than import across providers), and each package keeps its own self-contained toolchain (its own Foundry project) so it can build and be credited on its own. When the user asks "where should this integration live?", the expected answer includes the placement call AND how it wires into the app/UI and the execution layer. Confidence: 0.7
- Wants a minimal, generic dispatch layer as the middle tier between the shared domain vocabulary and the per-venue packages: a thin adapter/registry layer with no venue-specific logic, imported by the consuming service (`apps/execution`) to run the legs an agent defined — "a common generic execution adapter layer with a minimalistic middle layer that will then be imported to app/execution in order to run the particular execution legs based upon whatever agent has defined the intent." When several homes are coherent, prefers the smallest layer that reuses an existing, correctly-named package over creating a new module — chose `order-execution-layer` over adding a new module to `execution-domain`, explicitly to keep the domain package pure venue-free vocabulary. Confidence: 0.7
- Scales architectural effort to prize/sponsor value: the protocol-named-package rule applies to the tracks being competed for (Uniswap, 1inch, Arc), and a protocol that is NOT a prize partner does not earn its own package — the user explicitly refused to spend effort pulling Morpho into `packages/morpho` and directed the adapter stay inside `packages/oneInch`, accepting a known smell: "I don't want Morpho to put more effort into creating a separate package. Currently Morpho is not a partner in the prizes... Just keep it within the packages/1inch even though you are completely writing that it is not a good code smell or a best practice that we have defined." When a placement is kept as an accepted deviation, record it as such rather than re-arguing the smell. Confidence: 0.8
- Execution decisions must weigh RISK alongside cost: wants the risk parameters mined in `packages/risk-analysis-data-pipeline` presented in `apps/execution` next to the DeFi liquidity execution parameters, because "it is not just looking at the slippage or the gas fees it's going to entail. It's also the risk parameters" — so the agent can judge whether the strategy is sound. CORRECTION (risk-presentation pass): the threshold-triggered approval model originally implied here was explicitly overruled — a high risk reading is shown but must never block `/intent` or other endpoints, and the verdict is inferred by the agentic pipeline from metrics rather than thresholded in the service. A DEX-style cost-only view is insufficient for a held fixed-income position. Confidence: 0.75
- Wants a new cross-package integration's first increment to be deliberately light — "I just need a very quick idea of how you can use this risk analysis data pipeline with this workflow. I think for now it should be just ceremonial": present the data alongside for the agent to consume, prove the wiring is sensible, and defer hardening/automation to a later pass rather than building the full gated mechanism up front. Confidence: 0.7

eline collects the raw metrics per L2 chain (underlying-infrastructure risk, infrastructure dependency, exit characteristics, DeFiLlama data) and hands them to the agent/backend agent, which reasons over them with necessary context and tool calling to produce the risk verdict. A service that defines a 0–1 score with hardcoded thresholds and gates on it is explicitly wrong — "it should be a complete evaluation by our agentic pipeline based upon the metrics that were provided." Own-goal framing to avoid repeating: hardcoded ceilings are "a policy nobody set", and scoring without the strategy context is the mistake. Confidence: 0.9
- Risk output is PRESENTATION-ONLY and must never block execution: a high risk reading is still shown ("you can also showcase the result but don't block the /intent or the other endpoints"), surfaced as a notification or an add-on card within the execution component. Risk is an inference; whether to execute is a separate approval decision by the EOA holder — gating the second on the first puts a scoring bug in the path of funds. Confidence: 0.9
- Approval is required BY DEFAULT for execution: the reliance on the EOA account holder is deliberately encoded because traditional-finance workflow — especially large fixed-instrument investments — requires an accountable principal to authorise. A default that permits is a default that eventually permits something nobody chose; the execution side should default to requiring approval rather than asking the caller to remember to enable it. Confidence: 0.85
- Wants a configurable per-agent spend mandate: a setting (e.g., in a settings folder in `apps/agentic-EMS`) capping the maximum amount an agent may spend/approve per strategy or intent, with the execution side keeping enforcement simple (require approval by default, force approval when the mandate is exceeded rather than letting configuration waive it). Confidence: 0.75
- On long multi-phase work, wants the session state compacted into a durable handoff document so the remaining items can be finished without re-discovering anything: explicitly directed "you just have to maybe call the context compact. You need to compact the context with the relevant details in order to complete the remaining 8 items" while enumerating those items. The doc (e.g. `~/.commandcode/plans/<task>-CONTEXT.md`, a companion to the approved plan) is expected to lead with the expensive-to-find facts rather than prose — verified current state with its evidence, already-resolved interfaces, `file:line` anchors, root causes, verified data-source facts, binding taste constraints, the exact commands, and known failure modes — and to be good enough that the next pass starts editing immediately instead of re-reading files. Read it as a standing expectation for any long autonomous run that is approaching its context limit: compact at a green seam and carry the enumerated remaining task list forward rather than losing progress. Confidence: 0.8
- Approaches monorepo build-ordering problems at the dependency-graph level: expects a build/turbo failure mode to be fixed by declaring the correct dependency trees in the build tooling rather than by per-command workarounds — "you will be able to resolve it by defining the correct dependency trees. Make sure they don't collide with each other or create diamond patterns" — and points the agent at the official Turbo docs plus the repo's own root `turbo.json` as the references to verify the wiring against before concluding anything is wrong. Confidence: 0.6
- Prefers the simplest data-access path that works, and reuse over churn: when an existing package already covers the needed reach (e.g. fetching data across the major protocol categories and Polymarket), leave it alone — "You don't need to change it. Else do change whatever is needed" — and when a data need can be served either way, fetch it via conventional REST/traditional API methods rather than standing up a dedicated subgraph/Graph integration, reserving the specialized indexing path for cases where it is genuinely required. Confidence: 0.65
- Approves live third-party data calls at request time when the data drives an execution/strategy decision, rather than pre-snapshotting the feed into the repo first — confirmed the pool resolver may call the DefiLlama yields feed live, with the fetch timestamp (`scraped_at`) recorded as evidence, on the view that a stale pool list is worse than a network dependency for something that recommends positions. Confidence: 0.6
- One concept, one configuration variable: never add a second env var for something the schema already models — `VERTEX_PROJECT`/`VERTEX_LOCATION` were introduced for the embedding client and then removed once `GOOGLE_CLOUD_PROJECT`/`GOOGLE_CLOUD_LOCATION` were found to exist for exactly that purpose ("one concept with two names is how a deployment ends up setting the one nothing reads"). Before adding an env var, grep the existing env schema (and `.env.example`) for an equivalent key and reuse it, updating any requirement/omission messages and tests to name the real variable. Confidence: 0.75
- Configuration that is enabled but misconfigured must be REPORTED, never degraded silently: `LANGSMITH_TRACING=true` with no key still degrades to tracing-off (a service must not refuse to boot over telemetry) but now warns at boot naming `LANGSMITH_API_KEY` and is visible on `/health`, because that state is indistinguishable from working until someone notices an empty dashboard — "degrading is fine; degrading invisibly is not" (the debugging then lands on the instrumentation instead of the telemetry). Same disposition as the tool-dependency rule: a tool group whose dependency is absent is omitted and returned with the setting that would include it, rather than registered with a stub that errors on every call and makes the trace show a tool call instead of a missing capability. Confidence: 0.75
- Health/diagnostic endpoints are first-class acceptance surfaces: a deployed revision must state from a single unauthenticated curl what it actually is and what it was given — `/health` carries an `agent` block (impl, model, per-mode `{ tools, registered, omitted[{ id, reason }] }`) and a `tracing` block (enabled, key present, endpoint, project, optional project id, plus a `warning` for the enabled-without-key state). Secrets are reported only as presence, never as values — not even a key prefix — so the block stays safe to paste into a ticket or a screenshot, and a test asserts the payload contains no `lsv2_pt` fragment. Confidence: 0.75
- Read an existing artifact before writing or replacing it; when it already covers the ground, make targeted edits instead of shipping a wholesale rewrite. A freshly drafted `apps/inferrence/.env.example` was thrown away ("that file is already thorough — my draft would have been a regression") after the existing file was actually read, and only then were the two real gaps patched; the same read-first step preceded the tracing and env edits. Applies to env/config files the user maintains by hand and to any file being "regenerated". Confidence: 0.7
- Never fabricate a missing input to unblock work — state the blocker instead: asked to add a LangSmith API key so tracing could be verified independently, the honest answer was that no key was available, because a plausible-looking placeholder would make `/health` report `keyPresent: true` while every trace silently failed — a fresh instance of the exact silent-failure class being removed. Report what is missing and give the user an actionable way to see it took effect (set your own key, then `curl /health`). Confidence: 0.7
- Never hardcode a volatile opaque identifier in a template or config; carry a selector and resolve it against fresh live data, recording the resolution as evidence. Mandate legs hold a `poolSelector` (project/chain/symbols/exposure/stablecoin/minTvlUsd) rather than a pool UUID, because ids are opaque, chain-scoped and change on redeployment — a hardcoded one would be a snapshot of today rather than a standing instruction. Resolution uses one snapshot per cycle (the rate-limit discipline: N legs, one fetch), ranks by a 30-day mean rather than spot APY (a live Morpho vault returned 297,995%, which any spot ranking puts first), breaks ties deterministically on the id so the same snapshot always selects the same pool, and writes the resolved id plus `scrapedAt` into the run evidence so the choice is re-derivable. Confidence: 0.7
- Report non-fatal anomalies as explicit warnings instead of silently normalising or dropping them: leg weights that do not sum to 100 are reported (an operator who wrote 65/35 sees 65/35; one who wrote 60/30 is told, not quietly turned into 67/33) and a leg whose 30-day mean is under its own floor is flagged `belowFloor` rather than removed from the mandate. Unreported numeric fields are carried as `null`, never coerced to `0`, since a coerced zero is a different claim from "not reported". Corollary: a filter/search that matches nothing must fail loudly naming every filter applied as a funnel of survivor counts — "no pool matched" alone cannot be distinguished from a typo, and the last non-zero stage names the filter to relax. Confidence: 0.75

